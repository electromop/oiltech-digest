"""Prompts and JSON schemas for article processing."""

from __future__ import annotations


SUMMARY_INSTRUCTIONS = """Ты отраслевой аналитик нефтесервисных технологий.
Составь краткую суть статьи для внутреннего технологического дайджеста.
Пиши по-русски, без маркетинга, 2-3 предложения. Отрази: что произошло,
почему это важно для нефтесервиса/добычи и есть ли практический эффект.
Не добавляй факты, которых нет в статье.

Требования к языку:
- Пиши ТОЛЬКО на грамотном русском языке. Запрещено вставлять иностранные слова,
  буквы или случайные символы внутрь русских слов (нельзя «многоAgentная»,
  «наurvey», «térмального», «novizность»).
- Латиницу используй лишь для общепринятых аббревиатур (LNG, CCS, MPC, EOR, ИИ)
  и имён собственных/названий компаний. Английские термины переводи на русский
  (agent → агент, offshore → шельф, fabrication → изготовление).
- Числа указывай только если они есть в статье и значимы (суммы, объёмы, даты);
  не выдумывай и не добавляй незначимые цифры."""

TAGGING_INSTRUCTIONS = """Ты классифицируешь нефтесервисные новости.
Выбери один самый подходящий тег из списка. Если статья нерелевантна
нефтесервису, выбери ближайший верхнеуровневый тег с низкой уверенностью.
Ответ строго по JSON Schema."""

SCORING_INSTRUCTIONS = """Ты оцениваешь нефтесервисную новость для внутреннего
дайджеста. Оцени каждый критерий от 0 до 100 с учётом смысла статьи,
ключевых слов, источника и применимости. Итоговая оценка должна быть
взвешенной и объяснимой.

Объяснение (explanation) и обоснования по критериям (rationale) пиши ТОЛЬКО
на грамотном русском языке: без иностранных слов и случайных символов внутри
русских слов (нельзя «многоAgentная», «and архитектуру», «expected эффект»,
«novizность»). Английские термины переводи на русский; латиницу оставляй только
для общепринятых аббревиатур и имён собственных. Ответ строго по JSON Schema."""

RELEVANCE_INSTRUCTIONS = """Ты фильтр-редактор нефтесервисного дайджеста.
Определи, относится ли новость к нефтегазовой отрасли — по сути или по прямому влиянию.

relevant=true, если новость про:
- добычу, бурение, ГРП, заканчивание, КРС, промысловое оборудование, энергетику промыслов;
- цифровизацию/ИИ/роботизацию, R&D и технологии для нефтегаза (или явно переносимые в него);
- HSE в нефтегазе;
- рынок, цены, контракты, M&A нефтегаза и нефтесервиса (OFS);
- транзит и логистику поставок нефти/газа/СПГ: морские проливы и чокпоинты (напр. Ормузский,
  Малаккский, Баб-эль-Мандебский), трубопроводы, танкерные маршруты, экспортные терминалы;
- санкции, эмбарго, квоты, решения ОПЕК+ и геополитику, НАПРЯМУЮ влияющие на добычу,
  экспорт, транзит или цены нефти/газа.

relevant=false для спорта, культуры, развлечений, общих IT/стартапов и финансов без привязки
к нефтегазу, и для политики/международных отношений БЕЗ прямого влияния на добычу/поставки/цены
нефтегаза.

Геополитику не отсекай автоматически: если событие меняет транзит, поставки, доступ к ресурсам
или цены нефтегаза — это relevant=true. reason — кратко по-русски. Ответ строго по JSON Schema."""


SUMMARY_SCHEMA = {
    "name": "article_summary",
    "schema": {
        "type": "object",
        "additionalProperties": False,
        "required": ["summary"],
        "properties": {
            "summary": {"type": "string"},
        },
    },
}

TAG_SCHEMA = {
    "name": "article_tag",
    "schema": {
        "type": "object",
        "additionalProperties": False,
        "required": ["tag_id", "confidence", "rationale"],
        "properties": {
            "tag_id": {"type": "integer"},
            "confidence": {"type": "number"},
            "rationale": {"type": "string"},
        },
    },
}

RELEVANCE_SCHEMA = {
    "name": "article_relevance",
    "schema": {
        "type": "object",
        "additionalProperties": False,
        "required": ["relevant", "reason"],
        "properties": {
            "relevant": {"type": "boolean"},
            "reason": {"type": "string"},
        },
    },
}

SCORE_SCHEMA = {
    "name": "article_score",
    "schema": {
        "type": "object",
        "additionalProperties": False,
        "required": ["total_score", "score_label", "explanation", "items"],
        "properties": {
            "total_score": {"type": "number"},
            "score_label": {
                "type": "string",
                "enum": ["Низкая", "Средняя", "Выше средней", "Высокая"],
            },
            "explanation": {"type": "string"},
            "items": {
                "type": "array",
                "items": {
                    "type": "object",
                    "additionalProperties": False,
                    "required": ["criterion_id", "ai_score", "rationale"],
                    "properties": {
                        "criterion_id": {"type": "integer"},
                        "ai_score": {"type": "number"},
                        "rationale": {"type": "string"},
                    },
                },
            },
        },
    },
}
