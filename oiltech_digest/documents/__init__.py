"""Приём и разбор загруженных документов (PDF/PPTX/DOCX).

Отдельный пакет, а не часть ingestion: документ — не статья. У него нет источника,
нет URL и нет даты публикации, и он НЕ должен попадать ни в один из 44 запросов
репозитория по таблице articles.
"""
