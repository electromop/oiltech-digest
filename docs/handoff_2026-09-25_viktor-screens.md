# Хендоф 25.09.2026 — сессия F: экраны по документу заказчика

План сессии — `oiltech-agents/docs/handoff_2026-09-21_single-contour-sessions.md`, раздел «Сессия F».
Материалы заказчика (непубличные) — `~/Desktop/02_Инициативы/oiltech-docs/Материалы/Главная страница.docx`
(19.09) и замечания из чата 22.09. Ветка `feat/viktor-screens`, коммиты `9acc58e`, `a93cd8a`, `0ad5bc3`.

## Итог в одном абзаце

Источники — таблица с колонками заказчика; счётчики сходятся с таблицей: архив стал своим вердиктом
и своей плиткой. Теги свёрнуты, ключевые слова и стоп-слова — чипсами с крестиком. Скоринг — поля
друг под другом, кнопки на местах. Перевод: словарь знает «spud» и «granular», аудит ловит слова со
смешанным алфавитом, ядро прогоняет итог NL через словарь. Ревью двумя свежими сессиями нашло
3 бага UI и 2 дефекта словаря — исправлены, регрессии падают на старом коде.
pytest 658 (было 647), фронт 59 (было 47).
**PR electromop/oiltech-digest#68 влит (`2b7300a`), ядро выкачено на РФ 25.09 в 06:26 UTC,
критерии «готово» сверены на проде** (раздел «Выкат 25.09»).

## Выкат 25.09 — сделан и сверен

- **Перед выкатом:** сборок и выкатов на сервере нет, `running` 0, `queued` 0, доступно ~900 МБ.
- **Выкат:** `nohup sh scripts/deploy-core.sh app` в фоне с логом (`/root/deploy-f-20260925-0625.log`):
  - сборка;
  - страж ИИ-аренд: «в работе нет»;
  - перезапуск `app` 06:26:38–06:26:55 UTC, health ok;
  - `check-lanes: ok` — 4 воркера NL на `7e067c4` / контракт 1;
  - `refs/deploy/core` = `2b7300a`.

  Схема не менялась.

**Сверка (только чтение):**

| Критерий | Результат |
|---|---|
| Счётчик источников сходится с каталогом | экран 129 = 5 + 56 + 59 + 9; SQL: каталог 129, архив 45, отключены 9 |
| Новые поля | `articles_30d` есть у всех строк отчёта |
| Словарь на проде | `validate-terminology: ok`, 36 терминов, 41 замена, 15 примеров |
| Термины заказчика в карточках | 3 карточки со «спуд…» (#211456, #4965104, #6317665) починены точечно (`--article-id … --no-dry-run`); осталось 0 |
| Свежие статьи | задеплоенный `_core_glossary` на проде: «спудрил» → «забурил», «granularными» → «детальными», в том числе без «spud» в контексте; «под спудом» цело |

**Доля брака новым аудитом:**
- последние 1 000 статей — 18 полей (1,8 %), все — смешанный алфавит;
- весь корпус (14 215 статей с карточками) — 273 поля в 268 статьях (1,9 %): смешанный алфавит 245,
  «резервуар» 35, «спуд» 4, midstream 4.

**Массовую починку не запускал.** Пробный прогон по корпусу хотел поменять 242 поля: в основном
косметику «провел → провёл» и старое правило «резервуар → пласт». Это правило ломает смысл там,
где речь о хранилищах: «EPC для трёх резервуаров полного ограждения» (СПГ) → «для трёх пласт
полного ограждения». Правило надо сузить до контекста пласта — отдельная задача.

**Что нашёл детектор смешанного алфавита (245 полей)** — брак, который автозаменой не лечится:
- латинские буквы-двойники в русских словах: «вхoдит», «сбытa», «Танкeр», «Сантоc», «МoU»,
  «MГП» — такие слова не находит поиск;
- полупереводы: «глубокойwaterной», «электроэнergyю», «Пермian», «слotted», «Тупinамba»;
- склейка заголовков у Telegram-источников: «ИллинойсеExxonMobil», «сентябряOFAC», «Казаниhttps».
  Это парсер, а не перевод: первая строка поста склеена со следующей.

**Попутно замечено:** планировщик пересоздан в 05:33 UTC не мной, `restarts=0`. Судя по PR #67
(«init против зомби Chromium»), это параллельная сессия защиты планировщика. После снятия зависшего
Chromium в 05:08 в контейнере, вероятно, остались процессы-зомби: PID 1 (`scheduler-lock`) их не
собирает.

## Попутно: сбор MVP-1 стоял 20,5 ч (найдено при замере, снято 25.09)

- Планировщик завис 24.09 в 08:32 UTC. `cli parse --workers 3` ждал ответа Playwright, а тот —
  вкладки Chromium на `page.content()`: JPT «Инспекция и ТОиР», повторная попытка после таймаута
  `goto`. У `page.content()` таймаута нет. Браузерные источники идут последовательно в главном
  потоке после пула, поэтому одна мёртвая вкладка держит весь цикл. У шага планировщика потолка нет.
- Последняя статья — 24.09 08:29 UTC, новых задач нет с 08:00.
- Снято 25.09 в 05:08 UTC: погашен только главный процесс Chromium. Python получил
  `Page.content: Target … closed`, `parse` доработал штатно (05:09:10), цикл дошёл до `stats`.
  Контейнер и замок планировщика не трогались.
- За 14 дней это единственный такой простой. JPT-источники работают с 13.09, это ~400 циклов.
- Постоянная защита (потолок шага и дедлайн рендера) — отдельная сессия, её запустил владелец 25.09.

## Попутно: 37 источников молчат с ~19.09 (здесь не чинилось)

У 37 из 57 «застойных» последний материал собран ~19.09: 21 «разбор страницы», 16 браузер. Их
опрашивают каждый цикл, а `parse` пишет `источников ок=99, ошибок=0`. Примеры: Интерфакс ТЭК (27 за
30 дней), все JPT, BP, Shell, Лукойл, INPEX, JOGMEC. Разбирает отдельная сессия (запущена 25.09). На
экране это честно видно как «Требуют внимания: 57».

## 1. Источники

**Замер 25.09** (РФ, только чтение):
- всего 174: в каталоге 129, в архиве 45;
- состояния каталога: штатно 58, застой 57, 0 статей 5, выключены 9;
- все 45 архивных отчёт здоровья считал `disabled`.

Верхний счётчик показывал `filteredSources.length`, то есть без архива (19.09 — 133), а плитка «Все» —
`health.length`, то есть с архивом (173). Два разных набора на одном экране.

**Решение (утверждено владельцем 25.09):**
- «Всего источников» — каталог без архива, архив — отдельная плитка «В архиве»;
- плитки и таблица считаются по одному списку (`countSourceStates`);
- заказчику одна строка: «129 в работе + 45 в архиве = 174».

**Сделано:**
- Сервер: вердикт `archived` в `source_health_report` (раньше архив попадал в `disabled`), поле
  `articles_30d` — материалы за 30 дней по дате сбора. Список вердиктов —
  `repository.SOURCE_HEALTH_VERDICTS`, из него берут и API, и CLI.
- Экран — таблица с колонками заказчика: источник, тип, состояние, последняя загрузка, материалов
  за 30 дней, проблема. Всё, что было на карточке (сбор вкл/выкл, настройка, диагностика, архив,
  обратная связь), — в раскрытой строке («Настроить»).
- Подписи заказчика: Без материалов / Требуют внимания / Работают штатно / Отключены / В архиве.
  Источник подписей один — `SOURCE_STATES`.
- Формы добавления свёрнуты над таблицей. Фильтры «Статус» и «Покрытие» убраны: их заменяют плитки.
- `SourcesPage.tsx`: 676 → 467 строк (вынесены таблица, раскрытая строка, панель добавления, опрос задач).

**Порог «Требуют внимания» — 3 дня для всех** (решение владельца 25.09). С порогом по частоте было бы
18, но это спрятало бы обрыв ~19.09 у еженедельных источников.

**Открытый вопрос — колонка «Тип».** Заказчик только перечислил колонки. Сейчас показан способ
чтения: RSS-лента, разбор страницы, браузер, Telegram. Если он имел в виду тип издания из своего
реестра (Journal, Company…), поле `source_type` сначала надо чистить: 36 вариантов написания, у 38 из
129 — «RSS», «Discovered» или «manual». Уточнить у Виктора.

## 2. Теги

- Направления свёрнуты по умолчанию. В шапке каждого — сводка: «N подтегов · M ключевых слов».
  Есть «Развернуть все».
- «Родительский тег» → «Направление».
- «+ Подтег» → «+ Добавить подтег»: общий `ghostButton` под списком подтегов, а не рядом с «Удалить».
  Так же «+ Добавить направление».
- Ключевые слова RU/EN и стоп-слова (у подтегов тоже) — чипсы с крестиком (`KeywordChips`).
  Ввод принимает и столбик, и запятые — просьба заказчика 13.09. Повтор без учёта регистра не
  добавляется.
- «Сохранить» больше не прилипает к первой карточке: общий `.settingsHeader`.

## 3. Перевод

**Где живут замечания заказчика** (замер 25.09):
- «NDC 9 спудрил вертикальную скважину T-200» и «более granularными полевыми данными» — это
  сигнал радара 115 в базе агентов. Там же «ригов», «completion-операций», «офшорных».
- Радар прогоняет свой текст через `enforce_glossary_text` нашего модуля.
- В наших карточках MVP-1 нашлись «перед спудом» (#6317665) и «спудинг первой начался» (#4965104):
  2 из 18 статей со spud за 60 дней.
- «granular» в карточках MVP-1 нет: в 19 статьях за 60 дней он переведён.
- Аудит `audit-terminology` на 1000 свежих статьях до правки находил 0 проблем: словарь этих калек
  не знал.

**Сделано:**
- **Словарь.** Добавлены два термина:
  - `spud` → «забуривание». Глаголы «спудил/спудрил» → «забурил» с окончанием, существительные —
    по падежам, «спудинг … начался» → «забуривание … началось», «спуд-ин» отдельно. Идиома
    «под спудом» не трогается.
  - `granular` → «детальный». Латинский корень с русским окончанием: «granularными» → «детальными».
- **`warn_patterns`** — новое поле словаря: находка для аудита без автозамены. Нужно там, где
  замена сломала бы грамматику. Калька из `warn_patterns` в самом тексте включает свой термин, даже
  если английского слова в контексте нет. Такой была находка ревью: у доказательства сигнала 115 —
  обзор Westwood без «spudded».
- **Смешанный алфавит.** Аудит ловит слова, где латиница и кириллица смешаны без дефиса
  («granularными», «Тупinамba», латинская «c» в русском слове). Словарь для этого не нужен.
  «LNG-проект» и «CO2» не задеваются.
- **Словарь на ядре** при записи итога NL (`apply_process_result`, `apply_translate_result`).
  Воркер пересобирает владелец, и без этого правка словаря не доходила бы до новых карточек до
  пересборки NL. Повтор безопасен: ревью проверило идемпотентность на всех 123 примерах. Если
  прочитать статьи не удалось, итог пишется как пришёл.
- Золотые примеры — фразы заказчика дословно. `eval-terminology` проходит 123 из 123.

**Что это не чинит само:**
- Сигнал 115 — это данные Германа, их не трогаем.
- Новые сигналы радара получат словарь после D. Модуль общий, но у агентов своя копия, а радар
  исполняется на агентном NL.
- Подсказка модели в промпте (`glossary_prompt_block`) заработает после пересборки NL MVP-1.

## 4. UX скоринга

- Ключевые слова RU и EN — друг под другом во всю ширину.
- «Удалить» — внизу карточки справа, за линией (`.settingsCardFoot`).
- У шапки с «Нормализовать» / «Сохранить» появился отступ.
- Два профиля скоринга с вкладками — это сессия G.

## Чем проверено

- **pytest 658 passed.** Новые тесты:
  - вердикт `archived`, `articles_30d`, фильтр API;
  - словарь: фразы заказчика, «под спудом», «спуд-ин», текст без английского контекста,
    смешанный алфавит;
  - словарь на ядре и запись итога при сбое чтения.

  Один полный прогон дал 1 падение, повтор — 658 из 658. В это время тесты гоняли параллельные
  сессии. Какой тест упал, я не записал.
- **Фронт.** `npx vitest run` — 59 passed. `npx tsc --noEmit -p tsconfig.json` проверяет 55 файлов
  `src`, ошибок нет. `tsc -b` из `npm run build` `src` может не проверять: у корневого конфига
  `"files": []` и ссылки на проекты.
- **Регрессии на баги ревью** падают на коде до правки:
  - `SourcesPage.test.tsx` — архив «запирал» плитки;
  - `sourceUtils.test.ts` — «24.09 · сегодня»;
  - `TagsPage.test.tsx` — раскрывалось соседнее направление.
- **Глазами.** Временный стенд: страница с подменённым API и данными по мотивам прода, без входа.
  Экраны проверены на 1440 и 375 px. На телефоне ширина документа 375 = окну, таблица листается
  внутри своей обёртки. После правок ревью теги и скоринг проверены ещё раз. Стенд удалён,
  `launch.json` агентного репозитория возвращён. Для повтора: `frontend/f-preview.html` с
  `src/fPreview.tsx`, где `window.fetch` отдаёт фикстуры, и Vite на свободном порту.

## Ревью (две свежие сессии, `/code-review`)

**Спека**

Граница Германа не тронута. Решения ревьюера по находкам:
- «Всего = 129» — отклонение от буквы документа; утверждено владельцем.
- Словарь на ядре — сверх спеки; назван в PR.
- «Тип» — открытый вопрос.

Исправлено:
- «спуд-ин» → «Забуривание-ин»;
- калька не чинилась без английского «spud» в контексте;
- новый стиль кнопки вместо общего.

**Стандарты**

Жёстких нарушений нет. Исправлено:
- 3 бага: запертый архив, календарные дни, ключ новой группы тегов;
- вердикты перечислялись вручную;
- дубли типов форм;
- дубль `.header` и файл ради одного правила (`Scoring.module.css` удалён);
- мёртвый CSS: `.sourceCardGrid`, `.sourceArchiveSection`, `.typePill`;
- три места подписей состояний сведены в одно;
- сценарии экранов вынесены из `App.test.tsx` (1127 строк) в свои файлы.

Не исправлено:
- `external_ai.py`: 612 → 650 строк — уже был больше лимита.
- `.sourceName` оставлен: им пользуется `SourceCandidatesPage` Германа в агентном контуре.

## Выкат

Схема не менялась, поэтому `--schema` не нужен.

**Ядро (РФ)** — после мержа PR:

```bash
cd /root/oiltech-digest && sh scripts/deploy-core.sh app
```

Нужен только `app`: экран, API и запись итога NL. Планировщику и воркерам правки не нужны.

**Сверка после выката** (только чтение):
- счётчики экрана против SQL: `repository.source_health_report(limit=1000)` → вердикты; сравнить с
  `SELECT count(*) FILTER (WHERE archived_at IS NULL), count(*) FILTER (WHERE archived_at IS NOT NULL) FROM sources`;
- `docker exec oiltech_app python -m oiltech_digest.cli validate-terminology`;
- аудит всего корпуса новым словарём:
  `docker exec oiltech_app python -m oiltech_digest.cli audit-terminology --limit 40000 --show 40`;
- пробный прогон починки: `… repair-terminology --limit 40000 --show 20`. Он по умолчанию
  пробный, запись — только с `--no-dry-run`.

**NL MVP-1** — `deploy-nl.sh` у владельца. Нужен только для подсказки модели; детерминированные
замены уже делает ядро.

## Не сделано / следующий шаг

1. **Сверка — сделана**, см. «Выкат 25.09». Заказчику одна строка: «129 в работе + 45 в архиве = 174».
2. Колонка «Тип» — вопрос Виктору.
3. **Словарь.**
   - Сузить «резервуар → пласт» до контекста пласта: сейчас оно портит хранилища, а массовая
     починка из-за этого опасна.
   - Брак со смешанным алфавитом (245 полей) лечится перегенерацией карточек. Склейку заголовков
     Telegram надо чинить в парсере (`telegram_parser`).
4. Пересборка NL MVP-1 (`deploy-nl.sh`, владелец) — для подсказки модели. Удобно одним заходом после
   влитого PR #67, если он тоже меняет воркер.
5. Две параллельные сессии 25.09: защита шага планировщика (PR #67 влит) и 37 молчащих источников.
6. D принесёт F в агентный контур.
   - `SourceCard.tsx` переименован в `SourceDetails.tsx`: при слиянии берётся версия MVP-1.
   - Классы `.tagRowRoot`, `.tagRowChild`, `.tagChildrenList`, `.typePill`, `.sourceCardGrid`,
     `.sourceArchiveSection` удалены: ими пользовались только старые копии наших экранов.
7. Сигнал 115 в базе агентов (Германа): после D его можно поправить `enforce_glossary_text` —
   решение Германа.

## Ловушки и ложные версии

- **«Аудит 0 из 1000 — значит, терминов нет»** — ложь. Аудит видит только то, что знает словарь. До
  правки он не видел ни «спуд», ни «granular», ни смешанного алфавита.
- **`eval-terminology` генерирует проверки из каждого `forbidden_ru`**: «…термин {bad}…» → должен
  появиться `preferred_ru`. Глагольные кальки («спудил») туда класть нельзя: они чинятся в «забурил»,
  а не в «забуривание», и проверка падает. Для них — `phrase_repairs` плюс `warn_patterns`.
- **`phrase_repairs` применяются только при относящемся к делу термине.** Раньше это значило только
  одно: английское слово есть в статье. Теперь термин включает и калька из `warn_patterns` в тексте.
- **`\b` в Python-регулярках работает с кириллицей.** Но `\bспуд\b` срабатывает перед дефисом:
  «спуд-ин» → «Забуривание-ин». Нужен `(?!-)`.
- **`?screen=tags` не открывает экран** (`URL_ADDRESSABLE` в `App.tsx`). Тест через `App` шёл в
  ленту — сценарии экранов теперь рендерят страницу напрямую.
- **`git mv` неотслеживаемого файла падает**, и цепочка команд после него продолжается. Так стили
  разъехались по двум файлам. Проверять `git status` после каждого переименования.
- **`tsc -b` при `"files": []`** может не проверять `src`. Проверять `tsc --noEmit -p tsconfig.json`
  (`--listFiles` показывает, что проверено).
- **Стиль `ghostButton` у «+ Подтег» был и раньше.** «Отстранённой» кнопку делали место (рядом с
  «Удалить», вдали от подтегов) и подпись, а не стиль. Свой пунктирный стиль ревью справедливо
  назвало отступлением от «как и другие кнопки».
