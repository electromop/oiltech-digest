# Хендоф 2026-08-20 — фича «приём файлов»: заострение идеи до спеки

Сессия `/grill-with-docs`. Ничего не реализовано: ни кода, ни зависимостей, ни миграций.
На сервер не ходили. `BACKLOG.md` не тронут.

Исходное: направление №5 со встречи с Родионовым 09.07 («загрузка материалов»).
Отдельной задачей в `BACKLOG.md` по-прежнему НЕ заведено.

---

## Что решено (Р1–Р11)

| # | Решение |
|---|---|
| **Р1** | Ответственность за класс загружаемого материала — на пользователе. Аттестация — галочка, хранится в привязке к пользователю; штамп **на каждую загрузку**, а не разово в профиле. Путь загрузки идёт по существующему маршруту: OpenAI через внешний воркер (Амстердам). С9-3 (РФ-LLM) фичу **не** блокирует |
| **Р2** | Выход разбора = **карточка документа**: паспорт (тип, издатель, дата, язык, объём) · «о чём» по разделам · факты и числа с единицей, контекстом и **номером страницы** · сущности (компании, технологии, регионы) · разделение заявлений и фактов. **Плюс балл** отдельным блоком |
| **Р3** | Балл документа — **своя шкала**. Не сопоставим с баллом статьи, порог 60 к нему не применяется, документы в дайджест не идут, в UI помечен иначе |
| **Р4** | Отдельные сущности **`documents` / `document_cards`** — зеркало существующей пары `articles` / `article_cards`. Ни один из 44 запросов по `articles` документа не видит |
| **Р5** | Документы **личные по умолчанию**. Публикация в общий корпус — отдельное явное действие, и оно же вторая аттестация («имею право показать команде») |
| **Р6** | v1 = **сразу корпус**: пакетная загрузка + экран «Материалы» с группировкой по направлению и компании |
| **Р7** | Идентичность компании — **справочник с алиасами**; модель предлагает привязку, неуверенные случаи идут человеку на подтверждение. «Направление» — переиспользуем существующее дерево `tags`, второй справочник не заводим |
| **Р8** | Оригиналы файлов — на **общем docker-томе**; текст и карточка — в Postgres. Задача **№17 расширяется**: кроме `pg_dump` — архив тома |
| **Р9** | Направлений у документа **несколько, с долей покрытия**, одно помечено основным. Группировка списка — по основному, поиск и бенчи — по всем |
| **Р10** | v1 — **только веб-загрузка**. Почта и telegram отдельными задачами, после обратной связи |
| **Р11** | Сверка каждого извлечённого числа с текстом указанной страницы — **часть конвейера**, не тест. Не подтвердилось — факт помечен и в бенчи не идёт |

---

## Что доказано, а не заявлено

### Новостной скоринг к документу неприменим — арифметика

Считано по коду (`pipeline.py`, `prompts.py`, `config.py`), не по ощущению.

**Асимметрия входа.** Модель видит `_compact(raw_text, 6000)` — первые ~5% 60-страничного
документа. А keyword-половина балла считается по `_search_text(article)` (`pipeline.py:541`),
где `raw_text` берётся **целиком, без обрезки**. Две части одного балла смотрят на разные
объёмы текста.

**Насыщение keyword.** `keyword_score_for_criterion` — счёт присутствия, не плотности:

```
критерий с  5 ключевыми словами: 3 совпадения где угодно в тексте → 100
критерий с 20 ключевыми словами: 5 совпадений                     → 100
критерий с 40 ключевыми словами: 7 совпадений                     → 100
```

**Завышение.** `final_score = max(ai_score, 0.2·kw + 0.8·ai)`. При `kw = 100` это пол:

```
ai=30 → 44     ai=50 → 60  ← ровно порог дайджеста
ai=40 → 52     ai=60 → 68
```

Модель прочитала первые 5% отчёта, сочла посредственным (50) — документ выходит с 60.
Не по содержанию, а **по длине**. Тот же класс дефекта, что инцидент 06.2026 с блендингом,
только с обратным знаком: там формула топила балл, здесь надувает.

**Семантика.** `SUMMARY_INSTRUCTIONS`: «Отрази, **что произошло**». `SCORING_INSTRUCTIONS`:
якоря 85–100 = «промышленное внедрение, измеримый эффект», плюс абзац, различающий
«статья описывает ПРОБЛЕМУ, а не улучшение». Обзор рынка не является ни событием, ни
проблемой, ни улучшением — режима для такого входа у промпта нет.

**Отсюда три обязательных отличия скоринга документа** (Р2+Р3): вход — свёртка документа,
а не первые 6000 знаков; keyword-блендинг выключен (`final = ai_score`); якоря переписаны
под «применимое знание» вместо «событие». Список критериев из `scoring_criteria` при этом
остаётся тот же — меняются вход, формула и якоря.

### Синтетический источник в `articles` обошёлся бы в 37 функций

`oiltech_digest/db/repository.py`: **44 запроса в 37 функциях** обращаются к `articles`.
Документ, положенный туда, автоматически попадает в:

| Функция | Что произошло бы |
|---|---|
| `get_articles_needing_full_text` | `WHERE a.url IS NOT NULL AND full_text_status IS NULL` → фетчер пойдёт скачивать `upload://<hash>` |
| `get_articles_needing_summary` | новостная суть «2–3 предложения, что произошло» по первым 6000 знакам |
| `get_articles_needing_relevance` | гейт может поставить `relevant=false` → **документ пользователя тихо исчезнет из его же списка** |
| `get_articles_needing_tags` / `_scores` / `_title_ru` | новостные стадии, признанные неприменимыми |
| `digest_candidates` | документ становится кандидатом в выпуск вопреки Р3 |
| `cross_dup_candidates` | дедуп №21 сравнит документ со статьями по эмбеддингам |
| `get_articles_for_recheck`, `all_article_ids`, `mark_article_for_deletion`, `purge_pending_deletion` | массовые операции. №24 три недели назад скрыла 455 статей одной операцией |

Плюс синтетический источник встал бы в `sources` и был бы навсегда `stale` в здоровье
источников (он ничего не публикует по расписанию).

### Стоимость разбора

По прайсу из `config.py` (`gpt-5.4` = 2.5/15 USD за 1М токенов):

```
60 страниц ≈ 120 000 знаков ≈ 34 000 вх. токенов, 5 чанков map-reduce
вход ~44 000 ток., выход ~4 200 ток.  →  $0.17 за документ
корпус из 200 документов  →  ~$35 разово
для сравнения: установившийся режим всей системы — $35–40 в неделю
```

Разовый разбор дёшев. Дорого станет только на регулярной переобработке корпуса.

---

## Жёсткие предусловия

Обе задачи существуют независимо от фичи, но с ней становятся блокирующими.

**№17 — крон бэкапов.** Сегодня потеря базы неприятна, но восстановима в принципе:
статьи собираются заново с источников. **Загруженный документ собрать заново нельзя** —
у пользователя его может уже не быть. По Р8 оригиналы лежат на томе, а готовая команда
крона из хендофа 03.08 делает только `pg_dump` — файлы на томе в него не попадают.
«Починили №17» не будет означать «документы защищены», пока в задачу не добавлен архив тома.

**С1-4 — закрыть открытую регистрацию.** Проверено: `api.py:350` `/api/auth/register`
без проверки прав, без инвайта, без env-флага. В `SPRINT_PLAN.md` статус 🆕. Сегодня цена
открытой регистрации — чтение ленты. С приёмом файлов любой человек с адресом сайта
заводит аккаунт и грузит файлы, тратя диск и деньги на OpenAI.

---

## Периметр (инженерные умолчания, приняты без отдельного вопроса)

- **Тип файла** — по сигнатуре, не по расширению и не по `Content-Type`.
- **Имя** — хранить под сгенерированным идентификатором; пользовательское имя не превращать в путь.
- **Лимит на файл** — 25 МБ (60-страничный PDF = 2–5 МБ; презентация с картинками до 20 МБ).
  Квота на пользователя и на систему — иначе пакетная загрузка из Р6 упирается в диск молча.
- **docx/pptx = zip** — ограничить суммарный распакованный размер, число элементов, отношение
  сжатия. Отдельно: внутренности OOXML — это XML, внешние сущности и DTD в парсере выключить явно.
- **Сканы** — отклонять с честным сообщением, OCR в v1 нет. Проверка по объёму извлечённого
  текста на страницу: у скана текстового слоя нет, парсер вернёт пустоту, а модель уверенно
  сочинит сводку из ничего. Это тот же класс дефекта, против которого стоит «страж
  принадлежности текста».
- **Где парсить** — в воркере, с таймаутом и потолком памяти, файл читается с диска.
  Не в `app`: разбор 60 страниц — пиковая память, а `app` держит веб-сессии на сервере,
  где свободно ~1 ГБ и задокументирован OOM (№10).
- **Том — общий с первой строки.** Урок 03.08: правки брендинга не доходили до выгрузки,
  потому что `app` писал в свою копию, а воркеры читали свою. Загруженные файлы — то же
  самое состояние: пишет `app`, читает воркер.

---

## Столкновение с планом — решение за владельцем

Сегодня 20.08, идёт С3 «Рассылка Дайджест БРБ» (11–22.08) с вехой ГПХ 31.08. По правилу 3
самого `SPRINT_PLAN.md` состав текущего спринта меняется только заменой.

Корпус документов (Р6) — блок работ размером со спринт минимум. Ближайшее место, куда он
влезает, — **С4 (25.08–05.09)**, где сейчас стоит архив HTML-снапшотов, тоже прямой запрос
Родионова от 01.07.

**Открыто: какой спринт двигаем.** Это не вопрос спеки, это вопрос плана.

---

## Расхождение запросов — названо вслух

| | Запрос владельца | Запрос Родионова (встреча 09.07) |
|---|---|---|
| Что | «пользователь загружает файл → получает разбор» | «материалы лежат локально → собрать → сгруппировать по направлению/компании → бенчи» |
| Продукт | персональный инструмент | корпус / база знаний |
| Каналы | сайт | сайт + почта + telegram |
| Видимость | — | общая (иначе бенча нет) |

Р6 берёт сторону Родионова по сценарию (сразу корпус), Р5 — сторону владельца по видимости
(личное по умолчанию), Р10 — сторону владельца по каналам. Корпус собирается только из
явно опубликованного. Это работает, но означает: **пустой корпус — нормальное состояние
первых недель**, пока люди не начнут публиковать. Ждать бенчей сразу после релиза не стоит.

---

## Глоссарий (кандидат в `CONTEXT.md`)

Файлов `CONTEXT.md` и `docs/adr/` в репозитории нет; по границе сессии не создавались.

- **Документ** — файл, загруженный пользователем под его аттестацией. Не статья: нет URL,
  нет источника, нет даты публикации в ленте.
- **Карточка документа** — результат разбора документа. Зеркало `article_cards`.
- **Факт** — извлечённая величина: значение, единица, контекст, номер страницы, признак
  подтверждения. Неподтверждённый факт существует, но в бенчи не идёт.
- **Аттестация** — подтверждение загружающего, что он вправе загрузить этот материал.
  Штампуется на каждую загрузку.
- **Публикация** — явное действие, переводящее личный документ в общий корпус. Вторая
  аттестация: «имею право показать команде».
- **Корпус материалов** — множество опубликованных документов; основание для группировки и бенчей.
- **Направление** — тег из дерева `tags`. У документа их несколько, с долей покрытия, одно основное.
- **Компания** — запись справочника с алиасами. Строка из документа привязывается к записи.
- **Балл документа** — оценка по своей шкале. Со скорингом статьи не сопоставим.

## Кандидаты в ADR

1. **Документы — отдельная сущность, а не синтетический источник в `articles`.**
   Трудно обратить (миграция + весь путь обработки), удивительно без контекста
   («почему не переиспользовали ленту?»), реальный размен: недели против дней до демо.
2. **Ответственность за класс данных — на пользователе; путь загрузки идёт через внешний контур.**
   Трудно обратить (это позиция по безопасности, а не код), удивительно (записка к ИБ
   утверждает обратное), реальный размен: скорость против пересогласования с ИБ.
3. **Своя шкала балла для документов.** Удивительно («почему две шкалы?»), реальный размен:
   единообразие интерфейса против осмысленности числа.

---

## Что осталось открытым (в спеку, не блокирует)

- Дедуп документов: точный хеш файла — да; «тот же отчёт в другом формате» — вне v1.
- Язык карточки — русский всегда, независимо от языка документа (как в дайджесте).
- Частичный сбой разбора: 5 chunk-вызовов, падение на 4-м. Промежуточные результаты чанков
  сохранять, иначе повторяем тех-долг T23 «батч всё-или-ничего» на оплаченной работе.
- Модель под разбор: расчёт сделан на `gpt-5.4`; выбор — пилотом на реальных документах.
- Числа лимитов и квот: размер файла, число файлов в пакете, квота на пользователя.
- Кто может загружать: все авторизованные (после закрытия регистрации по С1-4) или только admin.

## Правка, которую нельзя забыть

`docs/architecture_diagram_notes.md`, раздел «Что пересекает границу РФ», утверждает
со ссылкой на код: границу пересекают «только публичные отраслевые материалы с открытых
сайтов». После этой фичи фраза **станет неверной**. Переписать до того, как документ
прочитает ИБ. Заодно: файл не в git (untracked) — схемы `architecture_diagram.html` и
`architecture_dataflow.html` закоммичены `059262f`, а пояснительная записка к ним нет.

---

## Следующий шаг

Идея готова к `/to-spec`. Перед этим — решить, какой спринт двигаем (раздел «Столкновение
с планом»), и завести задачу в `BACKLOG.md` (в этой сессии не делалось: T11 — деплой
`reset --hard` стирает правки трекера).

---

# Продолжение того же дня: от решений до тикетов

## Что сделано

| Коммит | Что |
|---|---|
| `1470690` | конфигурация инженерных скиллов: `CLAUDE.md`, `docs/agents/*`, пять меток триажа на GitHub |
| `bb74a77` | документы фичи + записка к ИБ, лежавшая вне git с 02.08 |
| `a4c2a22` | пересчёт обращений к `articles`: 37 функций / 44 запроса вместо 27/32 |
| `4b3d6e0` | карта тикетов в ранбуке |

Опубликовано: спека — **#31**, тикеты — **#41, #32–#40**, рёбра блокировки заведены
нативными зависимостями GitHub. Ветка `feat/upload-docs` от `origin/main`.

## Что вскрыло адверсариальное ревью спеки

Пять независимых рецензентов по осям: соответствие решениям, покрытие открытых вопросов,
обезврежены ли ловушки, соответствие шаблону, опровержение фактов. Две блокирующие находки:

1. **Частичные результаты нельзя сохранять через отчёт о прогрессе.** Я написал в спеке,
   что можно. Отчёт о прогрессе несёт одно число; результат внешней задачи попадает в базу
   ровно один раз — при терминальном завершении. Нужно собственное хранилище частичных
   результатов, прецедента на внешнем пути в проекте нет. Нашли независимо четыре
   рецензента из пяти.
2. **Утечка была закрыта только в одну сторону.** Я запретил класть содержимое в исходящую
   нагрузку задачи, но сохранённый результат отдаётся клиенту тем же кодом, а администратор
   видит задачи любого пользователя. Конверт воркера несёт вывод модели — для документа это
   карточка, факты и цитаты. **Разведка эту половину не покрывала**: её ловушка про утечку
   говорит только про нагрузку, не про результат.

## Мои утверждения, оказавшиеся неверными

Записано, потому что все они успели попасть в документы и в разговор:

| Утверждал | На самом деле |
|---|---|
| 27 функций / 32 запроса по `articles` | **37 функций / 44 запроса** — первый замер ловил только `FROM`, пропускал `JOIN`, `INTO`, `UPDATE`, `DELETE FROM` |
| «В конвейере нет ни ретраев, ни бэкоффа» | у фоновых задач бэкофф **есть**, экспоненциальный с потолком; ретрая нет только у самого вызова модели |
| Красный тест фронта — унаследованный | это **регрессия ветки**: на основной ветке набор зелёный |
| Сборка идёт без проверки типов | штатная сборка проверяет типы; без них собирается только образ. И дефект этого рода типами не ловится в принципе |
| $0.17 за документ «на текущей модели» | расчёт сделан на одной модели, дефолт репозитория — другая, вилка **44×** |
| Проверка факта — «тот же приём, что против чужого текста» | приёма нет: существующая проверка сравнивает хеши тел двух статей, а не вывод модели с источником |

Урок тот же, что и 03.08 с соцсетями: **первая проверка гипотезы часто ловит подмножество**.
Замер надо строить так, чтобы он мог себя опровергнуть, а не подтвердить.

## Подтверждено владельцем

Лимиты 25 МБ на файл · 20 файлов в пачке · 2 ГБ на пользователя · 10 ГБ на систему.
Загружать вправе любой авторизованный — **после** закрытия регистрации. Предусловия
заведены тикетами. Нарезка укрупнена с 12 тикетов до 10.

## Что ждёт владельца

1. **Три реальных документа** — без них #32 не двинется, а он блокирует всю ветку разбора.
2. **Доступ к серверу** для #34 (бэкап архивом тома).
3. **Какой спринт двигаем** — вопрос открыт с грилинга.
4. **Модель разбора** — выбирается пилотом, вилка цены 44×.

## Свободный фронт

`#41` (ветка готова) · `#32` (парсер) · `#33` (регистрация) · `#34` (бэкап) — четыре тикета
без блокеров, можно брать параллельно.

---

# Ночь 20→21.08: фича построена, ждёт выката

## Готово и проверено

| Проверка | Результат |
|---|---|
| Python-тесты | **347 passed** (было 251) |
| Тесты фронта | **18 passed** (было 14, причём один был красный) |
| Проверка типов фронта | чисто |
| Сборка фронта | проходит |
| Экран глазами в браузере | загрузка живого файла → карточка с 32 фактами |

Закрыты тикеты **#41** (ветка готова), **#32** (парсер), **#35** (загрузка),
**#36** (текст с якорями), **#37** (конвейер и карточка). **#38** сужен до остатка —
балл документа.

## Замеры на живых документах, `gpt-5-mini`

| Документ | Объём | Время | Цена | Фактов | Подтверждено |
|---|---|---|---|---|---|
| Презентация, 13 слайдов | 10 418 знаков | 43 с | $0,025 | 32 | **32 (100%)** |
| Отчёт НИР, 94 блока | 22 960 знаков | 58 с | $0,033 | 25 | **24 (96%)** |
| Заключение экспертизы, 107 стр. | 104 431 знак | 164 с | $0,121 | 226 | **146 (65%)** |

## Три дефекта, которые вскрыл ТОЛЬКО живой прогон

Все три прошли бы и юнит-тесты, и офлайн-заглушку, и уехали бы на прод.

1. **Бюджет ответа съедало рассуждение модели.** 2496 reasoning-токенов, 0 выходных,
   клиент бросает ошибку — частичного ответа не бывает. Фича падала бы на каждом
   документе. Лечение: бюджет 8000 и усилие рассуждения `low` на извлечении.
2. **Сверка отбивала верные факты.** Не снимались `>`, `≈`, словесные квалификаторы
   и ярлык перед двоеточием («GPT-5-MINI: 0,56»). Ложный отказ обесценивает саму
   пометку «не подтверждено».
3. **Во фрагментах не было пометок номера страницы.** Промпт их ожидал — их не давал
   payload. Модель угадывала, и 30 «неподтверждённых» из 60 находились на другой
   странице. После пометок сверка выросла с 27% до 65–100%.

Урок ровно тот же, что 03.08 с соцсетями и что был в ревью спеки: **проверка, которая
не может провалиться, ничего не проверяет.** Офлайн-заглушка отвечала «как надо»,
потому что я сам её и написал.

## Отдельно: попытка упростить, которую отбили собственные тесты

Увидев массу ложных отказов сверки, я хотел просто отключить проверку единицы.
Четыре существующих теста упали и были правы: «барр» вместо «тонн» блокировать НАДО.
Правильным решением оказалась нормализация написаний с сохранением различия величин.
Тесты сработали как тормоз против моего же поспешного упрощения.

## Что осталось на владельце

1. **Явное «да» на доступ к серверу** — без него я к проду не притрагиваюсь.
2. **`OPENAI_DOC_MODEL=gpt-5-mini` в `.env` внешнего воркера** — файла нет в git.
3. Порядок выката и откат — `docs/upload-docs_deploy.md`.

---

# 21.08, 04:15 — промпты под отрасль сделаны, ВЫКАТ ЗАБЛОКИРОВАН

## ⛔ Прод не отвечает, и это не VPN

Независимая проверка со стороны (не через VPN этой машины) даёт
**`connect ECONNREFUSED 109.68.213.12:443`**. Это TCP-отказ, а не таймаут: хост в сети
есть, на 443 никто не слушает. Тот же пробник на постороннем сайте отвечает `OK`,
значит дело не в пробнике.

С мака при этом — таймаут и до РФ-ядра, и до Амстердама: VPN режет маршрут, это
известная ложная тревога 20.07. **Но здесь два разных факта сразу:** VPN мешает МНЕ
зайти по SSH, и одновременно веб-стек прода действительно не слушает 443.

Что нужно от владельца, по порядку:
1. **Выключить VPN** — иначе SSH до обоих серверов не идёт. Модель для разбора отсюда
   больше не нужна, выкату OpenAI с мака не требуется.
2. После этого я захожу, смотрю, почему упал веб-стек, поднимаю и выкатываю.

Управлять серверами иначе нельзя: подключённый MCP Timeweb умеет только создавать
приложения, управления существующими VPS в нём нет — проверено.

## Сделано за этот заход

**Модель.** `gpt-5.5-mini` НЕ СУЩЕСТВУЕТ — проверено списком моделей, доступных ключу.
Есть `gpt-5.5` (без mini) и `gpt-5.4-mini`. Взят `gpt-5.4-mini`; переключение на
`gpt-5.5` — одна строка, но дороже в 6,7 раза.

**Цены.** Сверено с прайс-листом OpenAI: `gpt-5-mini` в таблице проекта стоял 0.75/4.5
при публикуемых **0.25/2.00** — счёт завышался втрое. Добавлены `gpt-5.4-mini`
и `gpt-5.4-nano`: без них префиксный матчинг относил бы mini к ставке `gpt-5.4`.

**Промпты под отрасль.** Типы документа отраслевые (обзор рынка / техническое решение /
КП / отчёт по работам / проектная документация / регламент / научная статья).
Суть отвечает на три вопроса: что за документ, какую производственную задачу решает,
что следует практически. Приоритет величин — дебит, обводнённость, проницаемость,
проходка, стадии ГРП, МРП, наработка, стоимость метра проходки, CAPEX/OPEX.
Контекст факта обязан называть объект. Отраслевой словарь перевода. Отсев реквизитов
и служебной статистики файла.

**Замеры, `gpt-5.4-mini`:**

| Документ | Время | Цена | Фактов | Подтверждено |
|---|---|---|---|---|
| Презентация «Нефтесервисный радар» | 11 с | $0,012 | 20 | **100%** |
| Научная статья нефтегаз+ИИ | 17 с | $0,017 | 11 | **100%** |
| Заключение экспертизы, 107 стр. | 45 с | $0,071 | 63 | 52% |
| Контроль: руководство на кондиционер | 39 с | $0,047 | 109 | 62% |

**Контроль сработал:** суть непрофильного документа заканчивается фразой «Документ
к нефтегазовой деятельности отношения не имеет: это руководство…». Ложной отраслевой
рамки нет — ровно как требует инструкция.

**Экран.** Суть растянута на всю ширину. Первая правка НЕ сработала: `.panel p`
специфичнее одиночного класса. Замер в браузере: было 501 px против 962 px у сводки,
стало 962 = 962.

---

# 21.08, ~06:00 — интернет-ресерч, правки раздела, всё отложенное в тикетах

## Ресерч на публичных отраслевых документах

Российские источники отсюда недоступны — тот же зарубежный выход VPN, что блокирует
и сервер. Взяты международные, и это оказалось полезнее: они проверяют правило
«карточка по-русски независимо от языка документа».

| Документ (открытый доступ) | Объём | Цена | Фактов | Подтверждено |
|---|---|---|---|---|
| Patterson-UTI, инвесторская презентация 05.2026 | 27 стр. | $0,031 | 59 | **86%** |
| EQT, презентация результатов 1Q 2026 | 38 стр. | $0,058 | 132 | 65% |

Извлечено по существу: 95 активных буровых станков, EBITDA $220 млн, CAPEX ~$600 млн,
ставка Senior Notes 3,95%, free cash flow $1 832 млн, продажи 618 Bcfe.
Заявления без подтверждения отделены: «Заявляется, что free cash flow является рекордным».

**Дефект, который вскрыл только этот ресерч:** сводка писала «Во фрагменте 1–13 дан обзор
бизнеса» — наша внутренняя нарезка текста, вылезшая пользователю. Слово «фрагмент»
в сводке запрещено; место называется по-человечески. Проверено повторным прогоном.

## Правки раздела, обе с тестами

1. **Предупреждение о нечитаемых страницах.** Карточка показывает число якорей без
   текста — на заключении экспертизы это 67 страниц из 107. Без него карточка выглядит
   полнее, чем есть.
2. **Фильтр «только подтверждённые»** в таблице фактов. По умолчанию неподтверждённые
   ВИДНЫ: молчаливо спрятанная ошибка опаснее показанной. Фильтр нужен там, где строк
   за сотню.

Оба теста проверены мутацией — без фичи падают. Тесты: **349 Python, 21 фронт**.

## Всё отложенное — в тикетах, ничего не потеряно

| Тикет | Что |
|---|---|
| #38 | Балл документа по своей шкале |
| #39 | Направления и справочник компаний |
| #40 | Публикация в общий корпус |
| #42 | Частичные результаты разбора и перезапуск с середины |
| #43 | Пакетная загрузка и квоты |
| #44 | Разбор перенести из веб-процесса в фоновую задачу |
| #45 | Распознавание сканов (OCR) |
| #46 | Каналы почта и telegram |
| #47 | Прогресс разбора в карточке |
| #48 | Выгрузка чисел: копирование и CSV |
| #49 | Поиск и сортировка по списку материалов |

## ⛔ Выкат по-прежнему заблокирован

Прод не слушает 443 (независимая проверка: `ECONNREFUSED`), SSH до обоих серверов
с этой машины не идёт из-за VPN. Нужно выключить VPN — дальше я захожу, поднимаю
веб-стек и выкатываю.

---

# 21.08, 02:00 — ФИЧА НА ПРОДЕ И РАБОТАЕТ

Выкат выполнен владельцем через веб-консоль Timeweb (SSH не шёл из-за VPN на обоих
контурах). Проверено загрузкой реального отраслевого документа.

## Первый разобранный документ на проде

`Атон_Нефтегазовый_сектор_август_2026.pdf` — 1,0 МБ, 16 страниц, 29 081 знак.

| Поле карточки | Значение |
|---|---|
| Тип документа | **обзор рынка** (отраслевой тип, а не общее «отчёт») |
| Издатель | ООО «АТОН» |
| Язык | русский |
| Фактов | **42, подтверждено 38 (90%)** |
| Заявления без подтверждения | 4, отделены |

Сводка — шесть пунктов, **без слова «фрагмент»**: правка, найденная интернет-ресерчем
на презентациях Patterson-UTI и EQT, доехала до прода. Суть идёт во всю ширину.
Фильтр «показать только подтверждённые (38)» на месте.

## Что подтвердилось на живом проде

- отдельные таблицы документов созданы, `schema-check: ok`, всего таблиц 20;
- маршрут `/api/documents` отвечает 401 (есть и требует входа);
- том `/data/documents` смонтирован;
- внешний воркер в Амстердаме видит `OPENAI_DOC_MODEL=gpt-5.4-mini` и берёт задачи;
- сверка чисел с текстом страницы работает: 4 числа из 42 помечены неподтверждёнными.

## Моя ошибка, которую стоит помнить

Я объявил прод лежащим на основании ОДНОГО внешнего пробника, дважды давшего
`ECONNREFUSED` на 443. Настоящий клиент — воркер из Амстердама — всё это время получал
200, а `docker ps` показал `oiltech_caddy Up 2 weeks`. Сервер не падал вообще.

Это ровно та ошибка, от которой я предостерегал по инциденту 20.07: один канал молчит —
не значит, что сервер лёг. Правило на будущее: прежде чем объявлять прод недоступным,
получить подтверждение из ДВУХ независимых точек, и хотя бы одна должна быть настоящим
клиентом системы, а не сторонним пробником.

Вторая ошибка в ту же ночь: команда дампа использовала имя контейнера там, где
`docker compose exec` ждёт имя сервиса. Из-за конвейера `pg_dump | gzip > файл` код
возврата взялся от `gzip`, тот успешно сжал пустой ввод, `&&` пропустил дальше — и
`ls` показал «бэкап» в 20 байт. Проверять надо не факт создания файла, а его содержимое.

## Открытые тикеты после выката

Отложенное: #38 балл · #39 направления и компании · #40 корпус · #42 частичные
результаты · #43 пакетная загрузка и квоты · #44 разбор в фоне · #45 OCR · #46 почта
и telegram · #47 прогресс разбора · #48 выгрузка CSV · #49 поиск и сортировка ·
**#50 дата из имени файла** · **#51 таблицы документов в проверку готовности**.

Последние два заведены по наблюдениям с прода: паспорт показал «дата не указано»,
хотя месяц и год есть в имени файла (модели имя не передаётся), и `required_tables=9`
без таблиц документов.
