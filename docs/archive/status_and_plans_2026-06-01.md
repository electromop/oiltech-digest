# OilTech Digest — статус, затраты, проблемы, планы (2026-06-01)

## 1. Текущее состояние
- **Код:** коммит `d8731df` в ветке `main`, запушен на GitHub (`electromop/oiltech-digest`).
- **Новый конвейер** (`python -m oiltech_digest.cli process-full`): по каждой статье целиком
  full-text → суть → релевантность → тег → скоринг (не «все суммари, потом все теги»).
  Дочитывание полного текста (`fetch-full-text`) вшито в конвейер.
- **Локальная БД:** 410 свежих статей (за неделю; McKinsey удалён). Обработана половина — **208**:
  релевантно **146** / отсев **60** (~29%), теги **141**, скоринг **140**, осталось truncated **20**.
- **Модель:** `gpt-5-mini` (локальный `.env`). ⚠️ На сервере `.env` не в git — модель нужно выставить отдельно.

## 2. Затраты (OpenAI, gpt-5-mini)
- **Финальный датасет (208 статей, по записям БД): ≈ $0.53** (~$0.0026/статья, ~1.19M токенов).
  - summary $0.131 · relevance $0.106 · tagging $0.106 · scoring $0.188.
- **Полный расход за сессию:** выше — отменённые/сброшенные прогоны (записи удалены, но вызовы оплачены:
  первый full-base mini, отменённые half-прогоны, A/B nano-тест, тесты). Оценочно **≈ $1.0–1.5**.
  Точная сумма — в дашборде OpenAI (platform.openai.com/usage).
- **Оценка на масштаб:** ~$0.0026/статья. Напр. полная база ~800 статей ≈ **~$2** за полный прогон.

## 3. Проблемы
### Источники (полный отчёт — `docs/parse_report_2026-06-01.md`)
- **BLOCKED (WAF 403/401, 8):** BCG, Baker Hughes, Energy Voice, S&P / IHS (×2), Weatherford, РБК Энергетика, СберТех.
- **TIMEOUT (6):** Saudi Aramco, Газпром нефть, Татнефть, Белоруснефть, Минпромторг, Губкин.
- **CONN_ERR (3):** eLIBRARY, Росстандарт, УГНТУ. **HTTP 412:** CNPC.
- **McKinsey #88** — отключён (`enabled=false`), статьи удалены: full-text таймаутит (read timeout 20с×3).
- **Сервер (Timeweb):** на дата-центровом IP блоков будет БОЛЬШЕ, чем локально (~26). Корень — репутация IP, не код.
  Фикс: residential/rotating прокси, либо проверка egress/DNS. Диагностика: `scripts/server_connectivity_check.py` (запустить на сервере).
### Качество/прочее
- **20 статей** остались truncated (источник без извлекаемого тела / блок).
- **Остаточный «мусор» в тексте ~3.4%** (7/208) — одиночные символьные слипы (mini ≫ nano, где было повсеместно).
  Дожать до нуля можно крупнее моделью или пост-чисткой — опционально.

## 4. Планы
- **[ПРИОРИТЕТ #15] Платный прокси (residential/rotating):** сервер на дата-центровом IP не может
  парсить заблокированные источники (8 WAF/403 + таймауты) — БД на проде пустая. Подключить платный
  прокси, интегрировать в `http_client` (env-переменная прокси), протестировать на сервере.
  Это разблокирует сбор данных на проде. Закрывает корень connectivity (см. #12).
- **[#13] Стриминг-пайплайн:** parse и AI-обработка одновременно (producer-consumer: очередь + пул воркеров,
  аккуратное завершение). Сейчас — последовательно (parse, затем process-full по очереди).
- **[#12] Проблемные источники:** решить по BLOCKED/TIMEOUT/McKinsey — прокси / фикс фетча / отключить.
- **Деплой на сервер:** `git pull origin main` → `docker-compose up -d --build`;
  ОБНОВИТЬ `.env` на сервере (`OPENAI_MODEL=gpt-5-mini` + тарифы); прогнать `server_connectivity_check.py`.
- **Вторая половина базы** — обработать при необходимости (`process-full --limit ...`).

---
_Бэкап БД до переработки: `/tmp/oiltech_backup_20260601_095300.sql` (локально)._
