# Хендоф 2026-08-22 — ревизия статуса, швы трекеров, мост Telegram

Сессия по запросу владельца: подсветить статус дайджеста, проверить логику работы
со спеками и тикетами, подключить рабочий чат Telegram.

На сервер зайти не удалось — SSH не идёт ни до СПб, ни до Амстердама (маршрут до
`109.68.213.12` уходит в `utun9`, VPN мака). Всё ниже добыто снаружи.

---

## 🚦 Первое, что нужно знать

**Прод работает НЕ с `main`.** `feat/upload-docs` — 23 коммита впереди, не смержена,
и именно её код стоит на сервере. Документированная процедура выката —
`git fetch && git reset --hard origin/main`. Выполнить её сегодня значит **тихо
снести фичу приёма файлов**: команда отработает без единой ошибки, а таблицы
`documents`, `document_cards`, `document_facts`, `document_anchors` и файлы на
томе останутся сиротами.

Лечится мержем ветки в `main` до любого следующего деплоя.

---

## 📊 Что проверено и чем

| Что | Факт | Чем подтверждено |
|---|---|---|
| Веб-стек | 200, три ответа подряд | `curl /api/health` → `{"ok":true,"articles":24579}` |
| БД и схема | `database.ok`, `schema.ok`, `missing_tables: []` | `/api/readiness` |
| Очередь | `queued_or_running: 0`, `stale_running: 0`, `stale_finalizing: 0` | там же |
| Сбор | 18 116 (03.08) → 24 579 (22.08) = **+340/сут** | сходится с 400–470/будний день |
| Тесты | **349 backend** + **21 frontend** зелёные | прогон в `.venv` и `npm test` |

### Как доказано, что прод на ветке

Два независимых признака, оба снаружи и без доступа к серверу:

1. `/api/readiness` отдаёт `present_tables`, где есть `documents`, `document_cards`,
   `document_facts`, `document_anchors` — на `main` этих таблиц нет;
2. бандл прода `/assets/index-CTvggPuq.js` содержит `«Загрузить материал»`,
   `«Карточка материала»`, `«Материалов пока нет»` и 5 вхождений `/api/documents`.

**`/api/readiness` — эндпоинт без авторизации.** Это главный рычаг диагностики,
когда SSH недоступен: он показывает схему, состояние БД и зависшие задачи.

---

## 🧵 Швы трекеров: флоу работает, границы — нет

Флоу спек-и-тикетов настроен и реально отработал: `/setup-matt-pocock-skills`
20.08, трекер = GitHub Issues `electromop/oiltech-digest`, спека **#31** → тикеты
**#32–#41** с нативными зависимостями → выкат → **#42–#51** на отложенное.
Адверсариальное ревью спеки поймало две блокирующие ошибки. Это не декорация.

Ломается он на трёх швах.

**Шов 1 — конфигурация живёт только на ветке.**
`git ls-tree main -- docs/agents/` пусто. `git show main:CLAUDE.md` →
`fatal: exists on disk, but not in 'main'`. Сессия от `main` не видит, что трекер
это GitHub Issues.

**Шов 2 — три трекера не знают друг о друге.** В `BACKLOG.md` **ноль** упоминаний
GitHub Issues.

| Задача | BACKLOG.md | SPRINT_PLAN.md | GitHub |
|---|---|---|---|
| Крон бэкапов | №17 🆕 | С1-2 🆕 | #34 |
| Закрыть регистрацию | — | С1-4 🆕 | #33 |
| Приём файлов (на проде!) | **нет** | **нет** | #31 + 10 тикетов |

Последняя строка — самая дорогая: `BACKLOG.md` это документ для заказчика.
Спринт работы уехал на прод 21.08 и в картине Родионова не существует.

**Шов 3 — `SPRINT_PLAN.md` не трогали с 09.07.** По плану идёт С3 «рассылка почтой»
с вехой 31.08; по факту делался приём файлов, которого в плане нет. Развилка
«какой спринт двигаем» помечена открытой 20.08 и не закрыта.

Плюс: `CONTEXT.md` и `docs/adr/` не заведены. Расхождение словаря уже видно —
в UI «Материалы», в коде `documents`, в спеке «документ», в тикетах «материал».

---

## ⚠️ Открытые риски

| Риск | Оценка | Замечание |
|---|---|---|
| Деплой `reset --hard origin/main` снесёт фичу | 🔴 | см. первый раздел |
| Бэкапов по расписанию нет | 🔴 | №17 🆕 с 23.07; проверить не смог — нет SSH |
| Регистрация открыта (#33) | 🟠 | обойдено «загрузка только админу», дыра на месте |
| Локальный стек на маке тратит OpenAI | 🟡 | 170 прогонов/$0.14 за 21.08, ключ настоящий |
| SSH не идёт с мака | 🟠 | VPN; прод при этом исправен |

**Проверить то, что мне не видно** — одной командой без VPN:

```bash
ssh root@109.68.213.12 'docker ps --format "{{.Names}}\t{{.Status}}"; crontab -l; cd /root/oiltech-digest && git log --oneline -1'
```

---

## 📮 Мост Telegram

Построен `~/Desktop/05_Инструменты/tg-bridge` — читает рабочие чаты личным
аккаунтом через telethon и складывает сообщения в `data/<чат>/messages.jsonl`
плюс `recent.md` для чтения глазами. Замечания Виктора попадают под фильтр
«похоже на баг» и предлагаются как черновики GitHub-тикетов — заводятся только
по подтверждению владельца.

В `oiltech-digest` мост ничего не пишет и файлов внутрь не кладёт.

**39 тестов зелёные**, гоняются без Telegram и без ключей. Не запущен: нужен
разовый вход руками (`src/gen_session.py`). Порядок — в `_О_ПРОЕКТЕ.md`.

**Урок оттуда, применимый шире:** эвристику багов починил прогон, а не
рассуждение. Написанные «по уму» паттерны пропустили «в паспорте документа дата
не указана» — дословно тикет #50. Я перечислял слова-названия («баг», «ошибка»),
а неинженер жалуется формой «не делает того, что должно».

---

## ➡️ Что дальше, по цене ошибки

1. **Смержить `feat/upload-docs` в `main`** — снимает риск №1 и заодно чинит шов 1.
2. **Отразить приём файлов в `BACKLOG.md`** — заказчик не видит сделанного.
3. **Крон бэкапов (№17/#34)** — команда готова в хендофе 03.08.
4. Актуализировать `SPRINT_PLAN.md` и закрыть развилку «какой спринт двигаем».
5. Запустить мост и разобрать накопившееся от Виктора.

Память: `oiltech-status-audit-0822`, `tg-bridge-tool-0822`.


---

# Продолжение 22.08 — мерж, чистка репозитория, вложения в мосте

## ✅ Мерж выполнен, риск №1 снят

`feat/upload-docs` влита в `main` **fast-forward** (ветка содержала весь `main`,
история осталась линейной), `origin/main` продвинут `e55ab71 → ee27f91`.
Деплой по инструкции больше не может откатить фичу с прода.

Прогон после мержа: **349 backend + 21 frontend**, сборка через `tsc -b` проходит.

**Четвёртое подтверждение, что прод крутит именно этот код:** локальная сборка
смерженного `main` дала бандл `index-CTvggPuq.js` — имя считается из хеша
содержимого, и оно совпало с тем, что отдаёт прод.

## 🧹 Чистка репозитория

Разбор всех 218 отслеживаемых файлов. Перед удалением каждый проверен на входящие
ссылки — удалялось только то, на что не ссылается никто.

**Удалено (11 файлов):**

| Что | Почему |
|---|---|
| `digest_example_7.html` | 1.2 МБ, ноль ссылок в репозитории |
| `docs/reference/` (6 файлов) | email-пакет v1; код использует свой `digest_email_template.html` |
| `docs/images/` (4 PNG) | ноль ссылок; справка использует свои в `docs/help/content/assets` |

**Перенесено в `docs/archive/` (13 файлов):** десять хендофов июня–июля,
`PROJECT_MEMORY.md` (снимок на 21.05), `geodistributed_architecture_plan.md`
(переезд выполнен 19.06), `report_2026-07.md`. Корень `docs/` разгружен с 30
файлов до 17, знание сохранено.

**Проверено и НЕ тронуто, вопреки первому впечатлению:**

- `web/app.html` — **несущий**: `api.py:318,326` отдаёт его как fallback, когда нет
  сборки фронта, и на `web/` смонтирован `/static`;
- `docs/help/**` — живой контейнер `oiltech_docs` в `docker-compose.yml` монтирует
  эту папку; все шесть картинок используются;
- `data/seed/*.xlsx` — упомянуты в `Dockerfile` и `.dockerignore`.

**Ссылки:** перенос порвал две (`BACKLOG.md`, `README.md`) — обе починены. Заодно
убраны 12 абсолютных путей с чужой машины (`/Users/g.fink/...`) в архивном хендофе
от 06.06. Проверка по всем markdown: битых ссылок нет.

## 📋 Бэклог: обновлён и починен

Добавлена задача **№28 «Приём файлов»** со статусом ✅ и списком 13 открытых
тикетов — до этого целый спринт, стоящий на проде, в документе для заказчика
отсутствовал. №17 (бэкапы) поднят с P3 до **P1 🔴**: с приёмом файлов цена выросла,
статью можно пересобрать с источника, загруженный документ нет. В шапку добавлено
правило против расхождения трекеров.

**Попутно вскрыт и починен дефект самого файла.** Приложение читает `BACKLOG.md`
через `/api/backlog`, разбирая таблицу по колонкам. Пять строк (№21, 24, 25, 26,
27) имели **6–7 ячеек вместо 5**: в них перекочевала колонка «Где» из формата
таблицы техдолга, а в №21 и №24 текст ещё и разрывала неэкранированная труба
внутри `sha256(title|url)` и `grep resp.url|canonical`. Из-за сдвига колонок
приложение показывало у этих задач чужие статусы и мусор вместо дат — у №17 после
моей же правки в поле «обновлено» оказалось `wc -l`.

Строки приведены к пяти колонкам, лишние ячейки схлопнуты в описание с пометкой
«Где в коде». Контроль: сверкой длин с версией до правок подтверждено, что ни одна
строка не укоротилась и ни одна задача не исчезла. Статус №24 исправлен с 🔵 на ✅ —
задача закрыта 03.08, но ячейка осталась от промежуточного состояния.

Счётчики до и после: `new 24 → 21`, `in_progress 5 → 7`, `done 24 → 25`. Разница —
ровно те задачи, чей статус приложение читало неверно.

⚠️ **Правило на будущее:** в ячейку таблицы `BACKLOG.md` нельзя писать вертикальную
черту даже экранированной — разбор в `backlog.py` её не понимает. И нельзя
добавлять колонки в отдельную строку: заголовок задаёт пять.

## 🖥 Локальный стек погашен

`oiltech_app`, `oiltech_scheduler`, `oiltech_pg` остановлены (не удалены, данные
целы). `platform-db-1` — чужой проект, не тронут. Стек был поднят задолго до работ
по приёму файлов: логи планировщика идут с 20.07, то есть dev-окружение просто
забыли выключить. Тратило $0.13–0.39 в сутки настоящим ключом OpenAI.

Поднять базу для тестов: `docker start oiltech_pg`.

## 📮 Мост Telegram: вложения

Вопрос «как обрабатываются фото» вскрыл дыру: мост записывал только пометку
`[image/jpeg]`, **сам файл не скачивался** — скриншот бага был бы не виден вообще.
Исправлено:

- картинки и PDF скачиваются в `data/<чат>/media/`, полный путь печатается рядом с
  сообщением; видео и аудио не тянем, но помечаем «не скачано», чтобы пропуск был
  виден; потолок `TG_MEDIA_MAX_MB` = 20;
- **сообщение с картинкой считается кандидатом в баг даже без текста** — человек,
  показавший экран вместо слов, иначе терялся бы молча;
- альбом из нескольких фото помечается `grouped_id`;
- догон после перезапуска не качает уже скачанное заново.

Тесты моста: **52 зелёных**. Мост не запущен — нужен разовый вход владельца.
