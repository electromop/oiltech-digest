# Хендоф 23.09.2026 — сессия C: надёжность контура РФ↔NL

План сессии — `oiltech-agents/docs/handoff_2026-09-21_single-contour-sessions.md`, раздел «Сессия C»;
архитектура — `oiltech-agents/docs/adr/0001-single-contour.md`, п. 4–5. Ветка `feat/contour-reliability`,
PR electromop/oiltech-digest#64.

## Итог в одном абзаце

Все пять пунктов сделаны в коде и проверены тестами, пункт 1 — ещё и сквозным тестом на настоящих
процессах. Независимое ревью (две свежие сессии: стандарты и спека) нашло два дефекта — повторную
оплату ИИ на запасном пути остановки и `init-db` по умолчанию в скрипте выката; оба исправлены, как и
замечания по стандартам. **Не выкачено**: на РФ — ждёт решения владельца (по плану первой идёт B, её на
GitHub нет; SSH — только с «да»), NL — командами владельца после ядра.

## Что сделано

| Пункт | Коммиты | Суть |
|---|---|---|
| 1. Мягкая остановка | `1b7735f` + правки ревью | SIGTERM: новых задач не брать, текущим 30 с, затем стоп на ближайшем шаге и `release` со сделанным; шаг, зависший дольше срока, главный поток возвращает со снимком сделанного на последней границе шага; ядро записывает сделанное, вычитает из задачи, сразу `queued`, попытку не списывает |
| 2. Один планировщик | `b9b827a` | `scheduler-lock`: advisory lock на всё время жизни процесса; второй пишет, у кого замок, и ждёт; потеря соединения — шаги стоп, выход 75; конвейер за профилем `pipeline` |
| 3. Контракт версий | `71309cd` + правки ревью | `X-Worker-Build`/`X-Worker-Contract` в каждом запросе воркера; ядро помнит по контейнеру; флаг расхождения считает ядро одним правилом (`lanes.consumer_mismatch`) для сторожа, экрана и выката NL |
| 4. Пауза в простое | `23d68bd` | 3 → 6 → … → 30 с, на первой задаче снова 3 |
| 5. Скрипты выката | `ce17a99` + правки ревью | `deploy-core.sh <сервисы>` (РФ), `deploy-nl.sh` (NL, владелец), страж `live-ai-leases` |

Решения, которые принял я и которые стоит проверить глазами владельца:

1. **Частичный итог при остановке.** «Для уже записанных статей ИИ повторно не вызывается» выполнимо
   только так: воркер отдаёт сделанное (`partial`), ядро пишет его как при `complete` и вычитает из задачи
   (явный список — без обработанных, пакет «N статей» — N уменьшается). Сбор и документ частичного
   итога не отдают — возвращаются целиком (сбор ИИ не зовёт; итог документа — одна карточка).
2. **Замок на всё время жизни планировщика, а не на цикл.** С замком на цикл два экземпляра шли бы по
   очереди — без наложения, но со сбором и постановкой ИИ вдвое чаще, то есть тот же дубль 21.09.
3. **Схема при выкате — только явно.** Если `schema.sql` изменился между выкаченным и выкатываемым
   кодом, `deploy-core.sh` требует `--schema` или `--no-schema` и без выбора отказывает до сборки.
   `init-db` — не только CREATE/ALTER (моя ошибка в первой версии, поймана ревью): одна транзакция с
   бэкфиллами по всей `articles`, а `ADD COLUMN IF NOT EXISTS` берёт эксклюзивный замок даже на
   существующую колонку — на время прогона стоят лента и выдача задач NL. Для новой таблицы проще
   создать её вручную и выкатить с `--no-schema`.
4. **Сроки остановки:** 30 с на завершение + 60 с на шаг + 15 с на отчёт → `stop_grace_period: 120s`.
   Тест сверяет compose с `config.py`.

## Ревью (две свежие сессии, `/code-review`)

- **Спека, дефект 1 — исправлен.** Запасной путь остановки возвращал задачу без итога: пакет 50 статей,
  19 готовы, 20-я висит в вызове модели — 19 оплаченных уходили в повтор. Теперь на каждой границе шага
  в реестр кладётся снимок сделанного (`heartbeat(result)` → `worker_shutdown.checkpoint`), главный поток
  отдаёт его. Там же: `progress(90)` шёл, пока задача числилась «в работе», — отчёт теперь забирается
  сразу после возврата обработчика.
- **Спека, дефект 2 — исправлен.** `init-db` по умолчанию на живой базе и до стража — см. решение 3.
- **Спека: нет сквозного теста** — добавлен `tests/test_worker_stop_live.py` (uvicorn + воркер процессом +
  настоящий SIGTERM). Проверка «красного»: с воркером из `main` задача остаётся `running`, попытка 1.
- **Стандарты:** `external_worker.py` перерос 500 строк — механика остановки вынесена в
  `worker_shutdown.py` (одно состояние процесса вместо четырёх глобальных и счётчика рядом с реестром);
  ленивый импорт обработчиков (`2f470ff`) откатан как лишний для сессии C; правило «контракт не совпал»
  было в трёх местах (экран красил бы давно пропавший контейнер вечно) — теперь одно, на ядре; умолчание
  лимита пакета и счётчик сделанного — в `contract.py`; запрос версий — методом клиента воркера; лишние
  `dsn=` и `SCHEDULER_LOCK_KEY` убраны.
- **Оставлено сознательно:** в `contract.py` две темы (версии и остаток задачи) — обе про протокол
  РФ↔NL; общие куски двух shell-скриптов не вынесены — отдельная библиотека была бы лишним файлом.
- **Добавлено по следам правки:** `TypeError` из `heartbeat` в циклах ИИ теперь громкий — колбэк старой
  сигнатуры иначе тихо отключил бы и остановку, и отзыв аренды (класс 24.07).

## Чем проверено (команды и цифры)

- Весь набор: `.venv/bin/python -m pytest tests/ -q -p no:cacheprovider` — **601/601** (было 546); фронт
  `npm test` — 35/35, `tsc --noEmit` чисто. Новые тесты сначала падали на коде до правки.
- **Сквозной тест остановки** (`tests/test_worker_stop_live.py`, ~6 с): задача в `queued` за секунды,
  попытка не списана, записанные статьи вычтены из задачи, второй воркер доделал остаток, каждая статья —
  ровно один вызов гейта. Ручной прогон тем же способом (6 статей, модель 0,5 с на вызов):

  | | воркер из `main` (до) | новый воркер |
  |---|---|---|
  | задача после SIGTERM | `running`, аренда ещё 9 мин 51 с | `queued` через 4,4 с |
  | попытка | списана (1) | не списана (0) |
  | сделанное | потеряно: 2 оплаченных вызова | записано 3 из 6 |
  | повторные вызовы модели | будут после аренды | 0: второй воркер доделал 3 оставшиеся |

- Один планировщик: 6 тестов на настоящих процессах и замке Postgres (второй ждёт и называет держателя;
  замок переходит к ждущему; `pg_terminate_backend` — шаги остановлены, код 75; SIGTERM — шаги погашены
  < 5 с). `docker-compose config`: без профиля — `app bootstrap caddy db docs`, с `--profile pipeline` —
  плюс `playwright-worker scheduler tasks worker`.
- Контракт: `docker-compose -f docker-compose.external-worker.yml config` с `GIT_SHA=deadbee` — SHA доходит
  до сборки всех четырёх сервисов; вёрстка «Воркеры NL» снята со стенда на CSS приложения.
- Пауза: модель по часам — было 6 × 100 = 600 claim за 5 мин простоя (замер прода 21.09 — 582), стало
  6 × 10 = 60 во вторые 5 минут.
- Скрипты: `sh -n`, `shellcheck -s sh` чисто; 15 тестов настоящим `sh` с заглушками docker/git/sleep.

## Не сделано / ждёт

1. **Выкат ядра на РФ** — ждёт владельца: по плану сессий первой выкатывается B.
2. **Пересборка NL** — владелец, после ядра (команды ниже).
3. Проверяется только на проде: нет `running` с мёртвой арендой после пересборки NL, `check-lanes`
   показывает версии всех четырёх воркеров, в простое ≤ ~60 claim за 5 мин (замер «до» — на выкате).
4. Строка «Итог сессии C» в плане сессий агентного репозитория.

## Выкат

**РФ (первый раз — скрипта на сервере ещё нет):**
1. таблица версий заранее (одна новая таблица, существующие не трогает):
   `docker exec oiltech_pg psql -U oiltech -d oiltech_digest -c "CREATE TABLE IF NOT EXISTS external_worker_consumers (consumer TEXT PRIMARY KEY, queues TEXT[] NOT NULL DEFAULT '{}', build TEXT, contract INTEGER, first_seen_at TIMESTAMPTZ NOT NULL DEFAULT now(), last_seen_at TIMESTAMPTZ NOT NULL DEFAULT now())"`;
2. `cd /root/oiltech-digest && git fetch origin && git reset --hard origin/main &&
   scripts/deploy-core.sh --no-schema app scheduler worker playwright-worker` — дальше скрипт сам:
   сборка, страж ИИ-аренд, перезапуск, health, `check-lanes`.

Дальше хватает `scripts/deploy-core.sh <сервисы>`. После выката ядра до пересборки NL `check-lanes` звенит
`contract_mismatch` у всех четырёх воркеров — так и должно быть.

**NL (владелец, веб-консоль Timeweb):** `cd /root/oiltech-digest && git fetch origin &&
git reset --hard origin/main && scripts/deploy-nl.sh`. Контейнеры идут по одному; следующий — только когда
предыдущий отметился в ядре новой сборкой. В первый раз задачи в работе вернутся по аренде, как раньше:
старый воркер — `python` под PID 1 без обработчика SIGTERM, сигнал он игнорирует; скрипт узнаёт его (нет
команды `worker-versions`) и гасит за 10 с, а не ждёт 120 с ради того же SIGKILL. Со второго выката —
мягко.

**Проверки после NL (на РФ):**
- `docker exec oiltech_app python -m oiltech_digest.cli check-lanes` — четыре строки версий, без тревог;
- `docker exec oiltech_pg psql -U oiltech -d oiltech_digest -c "SELECT count(*) FROM background_jobs WHERE status='running' AND execution_region='external' AND lease_expires_at < now()"` → 0;
- `docker logs oiltech_app --since 5m 2>&1 | grep -c 'POST /api/external-worker/claim'` в простое → ≤ ~60;
- если пересборка попала на ИИ-пакет: у задачи `error_message` «Возвращена воркером (…); сделано до остановки: N».

## Ловушки и ложные версии

- **Мой ложный тезис «`init-db` — только идемпотентные CREATE/ALTER».** В `schema.sql` есть UPDATE,
  INSERT и DELETE, файл идёт одной транзакцией, `ADD COLUMN IF NOT EXISTS` берёт эксклюзивный замок и на
  существующую колонку (ревью проверило на локальной базе). «Раньше шло при каждом `up`» — да, но тогда
  стек стоял целиком.
- **Часы ВМ colima спешат и раз в минуту подводятся назад (~200 мс).** Тест «ядро не выдало задачу» падал
  2 раза из 25: только что созданная задача была «из будущего». Оба сбоя ровно через 60 с. Продукт это не
  задевает; тесты повторяют `claim` до 2 с.
- **macOS `/bin/sh` (bash 3.2) при `LC_CTYPE=C.UTF-8` считает байт `»` частью имени переменной:**
  `"«$svc»"` → `svc\xc2: unbound variable`. В скриптах — `${svc}`.
- **`python -m` берёт код из рабочего каталога процесса, а не из `PYTHONPATH`;** `sitecustomize`
  грузится раньше, чем `-m` добавит каталог. Проверять «старый воркер против нового ядра» — только
  с `cwd` старой копии.
- Шаг планировщика живёт в своей группе процессов: после `kill -9` обёртки он сиротой держит pipe stdout
  открытым, и чтение вывода висит. В тестах вывод — в файл.
- Локально нет плагина `docker compose`; есть `docker-compose` 5.1.4 из Homebrew.

## Для слияния в агентный репозиторий (сессия D)

- `external_worker.py` — версия MVP-1; в `_HANDLERS` добавить `source_candidate_evaluate` и
  `signal_discovery`. Новый вид задачи — это строка в `_HANDLERS`, срок в `_JOB_STALL_SECONDS`, ветка в
  `api._apply_external_result`, а при частичном итоге — ещё `contract.PARTIAL_KINDS`. Тест
  `test_every_job_kind_resolves_to_a_worker_handler` не даст забыть `_HANDLERS` для вида любой полосы.
- Обработчики радара получают `heartbeat=beat`; `beat(done=None)` принимает и вызов без аргумента.
  Циклы ИИ в `external_ai` передают итог — колбэк старой сигнатуры теперь падает громко.
- У пятого сервиса NL (`nl-agents-1`) нужны `build: *worker-build` и `stop_grace_period` — тест compose
  проверяет все сервисы.
- `lanes.AI_LANES` — все внешние полосы, кроме сбора: `external-agents` попадёт под страж выката сама.
- `docker-compose.yml`: у `tasks` профиль `pipeline` (сессия B может перенести его в `archive`).

## Следующий шаг

Решение владельца: выкатывать C до B или ждать B. Затем выкат ядра по разделу «Выкат», команды для NL,
проверки после NL.
