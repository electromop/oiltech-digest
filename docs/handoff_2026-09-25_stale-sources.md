# Хендоф 25.09.2026 — «57 молчащих источников»: корень по группам

Ветка `fix/stale-sources-0925`, PR — см. конец. Метод — `/diagnosing-bugs`: сначала проба, которая
краснеет на симптоме, потом гипотезы. Всё на проде — только чтение (кроме инцидента ниже).

## Итог в одном абзаце

«Синхронный обрыв 18–19.09» — в основном **не поломка, а артефакт**: 18.09 новые правила порядка
ссылок (2bac79c) дали разовый пересбор, и у ~38 редко пишущих источников в базу легло старьё и
навигация. На три дня они стали «свежими» (19.09 stale = 19), а 21–22.09 разом стали stale (25.09 — 57).
Настоящие потери нашлись поштучно, и главная из них системная: **ключ адреса 13.09 срезал query
целиком** и склеил все статьи сайтов, где номер статьи в query (Минэнерго, EIA, РГУ Губкина, Новатэк,
Лукойл, Anton Oil, ONGC). С 13.09 каждая их новая статья молча отбивалась как «дубль», а схема 13.09
ещё и спрятала уже собранные. Это исправлено в ветке, восстановление спрятанного — отдельной командой.
Остальные потери точечные (извлечение/предфильтр/порядок у конкретных сайтов) — описаны ниже,
в код не вносились.

## Проба — чем мерили

Путь сбора (`fetch` → `extract_candidate_links` / `extract_articles_from_feed` → статья → предфильтр),
но вместо `insert_article` — три его рубежа SELECT-ами и вердикт по каждому кандидату: `known` (адрес
уже в базе), `TOO_SHORT`, `PREFILTER`, `DUP_URL_KEY_SAME` (та же статья), `DUP_URL_KEY_OTHER` (склеена
с ДРУГОЙ статьёй — потеря), `DUP_BODY_HASH` (то же тело, что у статьи источника), `WOULD_INSERT`.
Плюс кандидаты за лимитом 12: позиция на странице и дата самой статьи — «свежее, но спрятано порядком»
против «старьё/навигация». Запуск на ядре: `docker exec -i oiltech_scheduler python - < probe.py`,
соединение в режиме `READ ONLY`. Различитель `SAME`/`OTHER` обязателен: без него «дубль по ключу»
выглядит одинаково для нормы и для потери. Скрипт был одноразовым и в репозиторий не вошёл (не
просили); его место — вердикты рубежей вставки в `source-diagnose`, которому их сейчас не хватает.

## Группы источников

| Группа | Источники | Доказательство (25.09) | Причина | Что сделано |
|---|---|---|---|---|
| **1. Склейка по ключу адреса** | Минэнерго 47, EIA 20, РГУ Губкина 114 (RSS); Новатэк 46, Лукойл 43, Anton Oil 23063, ONGC 23066 | Минэнерго 25/25 пунктов ленты `DUP_URL_KEY_OTHER`, EIA 17/18 (склейка с «статьёй» `detail.php?id=` без номера от 30.06), Губкин 257/260; свежие релизы Лукойла `?rid=740957` (22.09) и Новатэка `?id_4=7882` скачиваются, проходят предфильтр и отбиваются | `url_key` (48410ed, 13.09) = host+path **без query**; схема 13.09 спрятала «копии» | **Исправлено** в ветке + `repair-url-keys` (вернёт 308) |
| 2. Разовый пересбор 18.09 | Сургутнефтегаз, Mubadala, COSL, INPEX, JOGMEC, Jereh, AOGR, Б1, Яков, Equinor, PTAC, iFactory, NZTC, ISA, Интерфакс ТЭК, Газпром нефть, Минобрнауки, Cairn, ROGTEC | вставленное 18.09 — старьё: Rystad 7/7 опубликованы до сентября, Aker 8/8, Mubadala 9/12, Сургут — предупреждения о мошенниках 2022–2025; сейчас всё под лимитом уже в базе, за лимитом — навигация/старьё; ленты RSS — `SAME` | не поломка: новых статей у сайтов нет | ничего |
| 3. Извлечение берёт не тот блок → «то же тело» | Eni 75, SWPU 23065 | у Eni с каждой страницы извлекается текст виджета чат-бота EnergIA — 3177 знаков, одинаковых на всех; у SWPU — меню сайта, 549 знаков | первая такая «статья» легла в базу, дальше рубеж №24 молча отбивает всё | лог отказа (WARNING, коммит 2); извлечение — отдельная задача |
| 4. Предфильтр по обвязке | Сколково Energy 52 | статья рисуется скриптом; запрос видит описание кампуса (501 знак, «рестораны») и общий `og:title` сайта → шум `ресторан` | для `request` сайт — SPA | отдельная задача (playwright или другая лента) |
| 5. Порядок «датированные первыми» | Petronas 65 | свежайший релиз «30 Years in Turkmenistan» — первая карточка ленты (позиция 129, взятые — 130…163), без даты → в `ordered()` после 12 датированных, за лимитом; `WOULD_INSERT`, 8796 знаков | `listing_cards.ordered` ставит недатированных после всех датированных, даже при селекторе | отдельная задача |
| 6. Браузерная группа | BP, Shell, ADNOC, Aker, Baker Hughes, Rystad, Spears; JPT-разделы 24392–24399 | 18.09 вставлено старьё; JPT-разделы молчат по устройству — их статьи раньше забирает основной JPT (id 2, 25.09 +2), статья в базе одна на все источники; у BP/Shell/ADNOC свежайшая статья в базе 02–11.09 | **не проверено живой пробой**: браузер на ядре не запускал по указанию владельца (зона сессии зависания) | проверить пробой после выката #67 |
| 7. Сбор стоял 20,5 ч | все | `parse`, начатый 24.09 08:21, закончился 25.09 05:09 | зомби Chromium, `page.content()` без срока | **другая сессия**, PR #67 (влит) |

Интерфакс ТЭК (49) — отдельно: лента `interfax.ru/tags/ТЭК/` — это тег про безопасность объектов ТЭК
(ЧОПы, теракты), новых записей с ~16.09 нет. Свежие ссылки на странице — сквозной блок общей ленты.
Не поломка, а неудачная лента; вопрос настройки источника.

Состав «дублей≈3180» за цикл (сплошная проверка всех 23 локальных RSS-лент): ~1800 — те же статьи
повторно, ~1030 — уже скрытые, **~300 — немые потери группы 1**, 4 — «то же тело» (ДРТ, МФТИ).

## Что в ветке

| Коммит | Файл | Суть |
|---|---|---|
| 1 | `ingestion/normalize.py` | `url_key` = host+path+значимый query; срезаются только трекинговые параметры (`from`, `ysclid`, `gaa_*`, `utm_*` и метки рекламных систем) — список по замеру всех адресов прода; пары в порядке по алфавиту, без раскодирования |
| 1 | `db/schema.sql` | бэкфилл `url_key` (строки без ключа) тем же правилом; DDL нет |
| 1 | `ingestion/url_key_repair.py` + CLI `repair-url-keys` | пересчёт ключей у адресов с query; возврат в ленту **только** спрятанного схемой 13.09 (`pending_deletion` без `marked_for_deletion_at` и причины — ручная и гейтовая пометка их всегда ставит), если ключ не занят видимой статьёй, тело не повторяет видимую статью источника и это не двойник статьи, убранной человеком/гейтом. Сухой прогон по умолчанию; с `--apply` план и запись — одна транзакция под `LOCK TABLE articles IN SHARE ROW EXCLUSIVE MODE` (вставки ждут секунды, чтение не блокируется). Статья, чей новый ключ уже занят другим написанием (вставилось между выкатом и починкой), ключ не получает и выводится строкой «ДУБЛЬ В ЛЕНТЕ» — уникальный индекс цел. Повторный прогон пустой |
| 2 | `db/repository.py` | `insert_article` пишет WARNING при отказе по «тому же телу» (с обоими адресами) |
| 1–2 | тесты | `test_normalize` (4), `test_url_key_repair` (2: SQL≡Python — адреса строятся из самого списка параметров, проверено мутацией; восстановление — 8 вердиктов), `test_api_integration` (2: вставка релизов Лукойла, лог); на старом коде падали ровно на симптоме Лукойла |

**Ревью двумя свежими контекстами** (`/code-review`: стандарты и спека). Нашли и исправлено: гонка
«план в одном соединении, запись в другом» и вечный откат починки, если между выкатом и починкой
вставится другое написание выжившей статьи (теперь — одна транзакция под блокировкой и
`key_conflicts`); двойник статьи, убранной гейтом/человеком, вернулся бы в ленту (теперь
`twin_of_marked`); INFO о склейке по ключу после фикса шёл бы фоном каждые 30 минут — убран; текст
WARNING был слишком уверен; `btrim` в SQL срезал только пробелы, а `strip()` — любые пробельные;
тест SQL≡Python покрывал 2 имени из 10; тест починки вынесен из файла на 1800 строк; лог — отдельным
коммитом. Оставлено с причиной: `_old_rule_key` знает формат ключа (одно место), два похожих блока
отказа в `insert_article` (выносить рано); адреса без схемы считаются по-разному в Python и SQL —
так было и до правки, на проде таких нет.

## Выкат и восстановление — только после «да» владельца

1. Выкат ядра, по одному и не во время чужого выката: `cd /root/oiltech-digest && sh scripts/deploy-core.sh
   --no-schema app scheduler worker playwright-worker`. `--no-schema`: в `schema.sql` поменялся только
   бэкфилл строк без ключа, таких на проде нет. `insert_article` работает и в планировщике, и в приложении
   (итоги NL-воркера), поэтому оба.
2. Сразу после — сухой прогон: `docker compose run --rm --no-deps -T app python -m oiltech_digest.cli
   repair-url-keys`. Ожидание по сухому прогону 25.09 07:11 UTC (логика после ревью на данных прода,
   READ ONLY): **вернуть 308** (Губкин 177, Минэнерго 69, EIA 40, Новатэк 10, Anton 6, Лукойл 5,
   ONGC 1), ключей пересчитать 725, дублей в ленте 0; остаются скрытыми 870 помеченных гейтом (причины
   вида «Новость о вводе ветропарка…»), 106 трекинговых копий РБК, 56 с тем же телом (Новатэк 47 —
   варианты страниц списка), 11 двойников убранного гейтом (Новатэк 10, Губкин 1), 4 с занятым ключом
   (TechnipFMC). Все 308 уже прошли ИИ (149 релевантны) — повторной оплаты нет; в окне сентября 56,
   из них релевантных 12.
3. `--apply` — в том же окне. Если в выводе есть «ДУБЛЬ В ЛЕНТЕ» — другое написание статьи успело
   вставиться после выката: ключ у старой строки не меняется, решить вручную (спрятать одну копию).
4. Первый цикл после выката вставит то, что терялось с 14.09: EIA 3, Минэнерго 15, Губкин 45 статей
   (+ релизы Лукойла/Новатэка) — залп небольшой, гейт ~70 статей.

Пока восстановление не прогнано, видимые статьи этих сайтов держат старый ключ: другое написание уже
собранной статьи (схема, хвост) может пройти рубеж по ключу — у RSS тело на вставке — анонс, а у старой
строки уже полный текст, так что рубеж по телу его не поймает. Поэтому шаги 1–3 — в одном окне.

## ИНЦИДЕНТ — мой, 25.09 05:56 UTC

Гасил свою браузерную пробу (запустил за минуту до указания владельца «не лезь») «деревом процессов»,
посчитанным в awk. `if (a[pp[x]])` в awk **создаёт** ключ, и `for (x in a)` выдал все родительские PID
хоста (0, 1, dockerd, containerd-shim, postgres). `kill -TERM` успел дойти до главного процесса
Postgres агентного контура — `oiltech_agents_pg`: в его логе 05:56:29.857 `received smart shutdown
request`, остановка чистая (контрольная точка записана), Docker поднял его за ~1,2 с, восстановление
после сбоя не понадобилось; `oiltech_agents_app` здоров, ошибок в логе нет. Следующим аргументом был
`0` — своя группа процессов, шелл умер, остальные PID из списка сигнала не получили (время работы
dockerd, `oiltech_pg`, планировщика, sshd не изменилось — проверено). Перед этим `pkill -f "<строка>"`
убил мою же SSH-сессию: строка была и в её команде. Это зона Германа и действие без «да» владельца.
**Правило:** на сервере убивать только явный PID после `ps -o cmd= -p PID`; никаких вычисляемых
списков, `pkill -f` и `kill 0`.

## Ложные версии — чтобы не повторять

1. **«Синхронный обрыв = регресс 18–19.09».** Дата последней статьи испорчена пересбором: у редких
   источников «последнее» — это старьё, вставленное 18.09. Проверять не дату последней статьи, а дату
   публикации того, что вставилось, и есть ли на сайте свежее, что не вставляется.
2. **«`last_parsed_at` свежий — источник опрашивается».** 25.09 он у ~60 источников стоял на 24.09 08:21:
   сбор висел 20,5 ч.
3. **Короткое замыкание по `last_listing_hash`** — его нет с июня (adf7a14); хэш пишется только для
   диагностики.
4. **Моя оценка «зомби chrome не срочны, `pid_max` 4 194 304».** Предел — у cgroup `pids.max` = 2315,
   зомби его и съели (разбор сессии зависания).

## Дальше

- Выкат + восстановление (выше) — после «да».
- Браузерная группа (6): после выката #67 — проба в окне между циклами планировщика, по одному Chromium.
- Отдельные задачи: Eni (извлечение цепляет виджет чат-бота), Сколково Energy (JS-страница), Petronas
  (порядок недатированной первой карточки), лента Интерфакс ТЭК.
- Метрика stale после любой смены правил сбора врёт три дня подряд: смотреть на дату публикации, а не
  сбора. Диагностике `source-diagnose` не хватает вердиктов рубежей вставки (`OTHER`/`BODY`) — это и
  есть проба выше.
