# 📋 Бэклог OilTech Digest

Единый список задач, замечаний и пожеланий по проекту.

**Как пользоваться:** впишите свой комментарий в раздел [«📥 Входящие»](#-входящие--пишите-сюда) внизу — команда возьмёт его в работу, перенесёт в план и будет обновлять статус, чтобы вы видели прогресс.

**Статусы:** 🆕 новое · 🔵 в работе · ✅ готово · ⏸ отложено · ❌ отклонено
**Приоритеты:** **P1** (срочно) → **P2** → **P3**

---

## 🔜 В работе и план (по приоритету)

> 🗓 **План спринтов MVP2 (живой документ)** — [`docs/SPRINT_PLAN.md`](docs/SPRINT_PLAN.md): блоки работ, спринты до 18.12, схема, правила изменения.
> Подробный инженерный статус последнего спринта — [`docs/archive/handoff_plan_2026-07-20.md`](docs/archive/handoff_plan_2026-07-20.md).
>
> **➡️ Очередь работ (обновлено 22.08, вечер):** ближайшее — **№29 дайджест для ДСА к 03.09**, и он упирается в качество ленты: **№24 переоткрыта** (чужая суть продолжается, GitHub #52) → **№21 дедуп** → GitHub #53 невоспроизводимый балл, #54 пол балла, #55 заголовки, #56 молчащие источники. №11 закрыт решением, №28 приём файлов на проде.
>
> 🔗 **Второй трекер — GitHub Issues** [`electromop/oiltech-digest`](https://github.com/electromop/oiltech-digest/issues). Разделение: там — спеки и инженерная нарезка работ с зависимостями, здесь — задачи и статусы для заказчика. **Правило, чтобы они не расходились:** закрытый тикет отражается строкой в разделе «Сделано» этого файла. До 22.08 правила не было, и целый спринт (№28) в бэклоге отсутствовал.

| # | Приоритет | Задача | Статус | Обновлено |
|---|:---:|---|:---:|---|
| 1 | **P1** | **Перепроверка релевантности — ЗАВЕРШЕНА 23.07.** 83/83 батча, 8292 статьи, 0 ошибок, 7,2 ч, **$51.31** на gpt-5.4 + сжатых промптах (на старой связке было бы ~$108). Итог: скрыто всего 7403 = 5577 уже были скрыты + 1411 не гейтились раньше + **415 реально ушли из ленты**. Видимых осталось 6494, из них 929 со скорингом ≥60. Ни одна статья не удалена. ⚠️ Возобновлять такие прогоны ТОЛЬКО разностью множеств (`all_article_ids()` отдаёт id НЕ по возрастанию — ватермарк по max(id) пропустил бы 8000 статей) | ✅ | 2026-07-23 |
| 2 | **P2** | Дайджест: убрать блок «Аналитическая сводка»; исправить формат выгрузки; корп.шрифты GPN во всех блоках; фикс-картинка под тег | ✅ | 2026-07-01 |
| 3 | **P2** | Дайджест: полный редизайн «как у Виктора» (v1 готов, довести) | 🔵 | 2026-06-26 |
| 4 | **P2** | «Читать далее» в PDF — **код-фикс ✅ сделан** (`9d5d655` + PR#30): `_build_candidate_from_anchor` сохраняет query и финальный слэш, статьи больше не схлопываются в URL раздела; покрыт `tests/test_request_parser.py`. **ОСТАТОК (прод, не код):** разовый пере-сбор СТАРЫХ дайджест-статей, у которых ссылка уже сломана в базе — новые собираются правильно | 🔵 | 2026-07-25 |
| 5 | **P3** | Дайджест: E-mail-формат выгрузки + список получателей | 🆕 | 2026-06-26 |
| 6 | **P3** | Хранение дайджестов по годам/месяцам + метрики | 🆕 | 2026-06-26 |
| 7 | **P3** | UI «Теги»/«Скоринг»: удобнее (нужна конкретика заказчика) | ⏸ | 2026-06-26 |
| 8 | **P4** | Продуктовый лендинг через поддомен | 🆕 | 2026-06-19 |
| 9 | **P3** | Источники — **сверка 25.07 сжала задачу**: гео-роутинг опт-ин ✅ (`FETCH_EXTERNAL_ENABLED`, `network_policy.route_source_task`, `set-source-region`); Росатом и Сургутнефтегаз ✅ (оверрайды в ревизии 17.07: `request`+`listing_url`, Сургуту ещё анти-заморозка `request_parser.py:289`) — Росатом сделан через `request`, а не playwright, и это работает. **ОСТАЁТСЯ узко: Сбер/СберТех и Татнефть** — оверрайда нет вообще, парсят засеянную главную → навигацию; нужен `listing_url` на раздел пресс-релизов (+ playwright, если listing рендерится JS) | 🔵 | 2026-07-25 |
| 10 | **P1** | **Переезд на более мощные серверы.** РФ-core: 1.9 ГБ RAM, свободно ~116 МБ, задокументированный OOM при сборке (03.06), +контейнер трекера ужал запас. Нужен план: тариф/провайдер, перенос БД (дамп ~34 МБ), DNS + Caddy/Let's Encrypt, окно простоя, откат. NL-воркер оценить отдельно (он справляется, но SSH к нему трансгранично закрыт) | 🆕 | 2026-07-23 |
| 11 | **P1** | Решение по **7403 скрытым**: **РЕКОМЕНДАЦИЯ — НЕ делать `purge`.** `delete_article` сносит и строки `ai_processing_runs` статьи → уничтожит историю AI-затрат (только что почин. T10). Цель уже достигнута: скрытое не видно ни в ленте, ни в дайджесте; диск 27%; скрытых можно перегейтить, удалённых — нет. **✅ РЕШЕНИЕ ВЛАДЕЛЬЦА 02.08: скрытое остаётся скрытым, `purge` не делаем.** Модель «нерелевантное и испорченное прячем, но не уничтожаем» принята как общая — по ней же идут 662 из №24. Возврат к вопросу — только если диск станет узким местом; тогда рассматривать не `purge`, а развязку `ai_processing_runs.article_id` от каскада, чтобы аудит затрат пережил удаление | ✅ | 2026-08-02 |
| 12 | **P2** | Дайджест-полировка — **✅ закрыто сверкой 25.07** (все три пункта в коде): плейсхолдер по категории — `digest.py:505-517` + `_news_placeholder_data_uri` (SVG с палитрой и бейджем категории) + бэкфилл og:image; hero в DOCX — `_docx_hero_bytes` + `add_picture` (`digest.py:954`); GPN-шрифты — заметка «в HTML-превью Arial» **устарела**: с `2dda646` (01.07) `_embedded_font_faces()` вшивается и в HTML-письмо, **проверено на живом проде** (@font-face 797 КБ base64, GPN присутствует). Модель отбора — ручная курация (решение владельца 23.07). Мелочь на потом: устаревший комментарий `digest.py:795-796` и дублирующая вставка шрифта в PDF | ✅ | 2026-07-25 |
| 13 | **P2** | Вернуть OGJ + Offshore Magazine (403 от Endeavor на дата-центровый IP). **Сверка 25.07:** механизм гео-роутинга готов и оттестирован, но сами источники **НЕ** помечены `external` — пометку сознательно откатили в `248e1f9` («source-health опроверг аудит»). Порядок: (1) главный гейт — ЭМПИРИЧЕСКИ проверить, что NL-IP получает 200 с фидов `ogj.com`/`offshore-mag.com` (прод-проба, из кода не решается); (2) при 200 — пометить точечно ровно эти два источника; (3) убедиться, что на проде `FETCH_EXTERNAL_ENABLED=1`. ⚠️ Не возвращать пометку веерно «на всякий» — прошлый веерный external уже откатывали | 🆕 | 2026-07-25 |
| 14 | **P3** | Промпты сжаты 23.07. Остаток — у TAGGING основной вход это **список тегов** (2314 токенов/вызов): подавать срез вместо полного списка. **Подтверждено открытым 25.07:** `pipeline.py:369-382` строит промпт циклом по ВСЕМУ списку из `list_enabled_tags()` (без LIMIT), среза нет. Как делать: пре-фильтр тегов-кандидатов до LLM (по keyword-совпадениям через существующий `keyword_tag`/`_contains_keyword` либо по родительским веткам) | 🆕 | 2026-07-25 |
| 15 | **P3** | Отдельный тег под инциденты/геополитику (D19/D20) — код всегда форсит тег → инцидент садится в тех-направление и искажает аналитику. **Подтверждено открытым 25.07, обе части:** тегов D19/D20 нет ни в сиде (`data/seed/2_Направления…`, только D01–D18), ни в коде; форс-логика `keyword_tag`→`tags[0]` цела. Остаток: (а) завести теги «Инциденты»/«Геополитика», (б) разрешить «без тех-направления» вместо форса | 🆕 | 2026-07-25 |
| 16 | **P3** | Новое от заказчика (01.07, Родионов): сохранять HTML новости; «платные подписки — можем подключить?» | 🆕 | 2026-07-23 |
| 17 | **P1 🔴** | **Крон бэкапов БД — единственный риск, который нельзя обнаружить постфактум и нельзя откатить.** Дампы снимаются вручную и лежат на том же сервере, `crontab` пуст, последний дамп 22.07. **Цена выросла после №28: статью можно пересобрать с источника, загруженный документ — НЕТ, а `pg_dump` не покрывает файлы на томе** — на архив тома заведён отдельный GitHub #34. Готовая команда крона — в `docs/handoff_2026-08-03_otpusk.md`. ⚠️ Ловушка, на которую уже наступали 21.08: когда `pg_dump` пишет через конвейер в `gzip`, код возврата берётся от `gzip` — тот успешно сожмёт пустой ввод, и `ls` покажет «бэкап» в 20 байт. Проверять надо не факт создания файла, а его содержимое — распаковать и посчитать строки | 🆕 | 2026-08-22 |
| 18 | **P2** | **Раздел статистики** — ✅ сделано 24.07: экран «Статистика» для админов (воронка по месяцам, затраты на ИИ по моделям, активность по пользователям + ИТОГО), эндпоинт `/api/stats/monthly`, агрегации `monthly_platform_stats` / `monthly_ai_cost` / `monthly_user_activity`. Базлайн: слепок `/root/oiltech-backups/status_snapshot_2026-07-24_08-08.csv`. **Первое измерение вскрыло риск:** июль — собрано 9941 (×2 к июню), а годных в дайджест 221 против 644 в июне; средний балл 44→34. Расширение источников даёт объём, а не пользу | ✅ | 2026-07-24 |
| 19 | **P2** | **«Шум»/«Дубликат» → скрытие ✅** (24.07, подтверждено сверкой 25.07: `api.py:487` исключает `noise`/`duplicate` пер-юзерно, статусы в `user_article_states`, попадают в статистику). **ОСТАТОК — защита от глобального удаления:** сверка показала, что механизмы РАЗВЯЗАНЫ — `mark_article_for_deletion` (`repository.py:1746`) гейтит только по `monthly_digest_items` (сохранённый дайджест) и **не смотрит пер-юзерные статусы вообще**. Значит чужой `review`/`digest` статью не защищает. Нужен guard `NOT EXISTS (SELECT 1 FROM user_article_states WHERE article_id=%s AND status IN ('digest','review'))` в `mark_article_for_deletion`/`delete_article`. Разметка ЗАКАЗЧИКА (Родионов, id4) авторитетна. Та же оговорка действует для №21 (near-dup) | 🔵 | 2026-07-25 |
| 20 | **P2** | **Разобрать 112 «шумов» Родионова как обучающий материал гейта.** Это прямая обратная связь заказчика о промахах отбора (растёт: 39 в июне → 67 в июле). Найти системные паттерны и внести в правила промпта | 🆕 | 2026-07-24 |
| 21 | **P2** | **Семантическая дедупликация (near-dup).** Сейчас ловятся только ТОЧНЫЕ дубли: `content_hash=sha256 от заголовка и URL` = повторный парсинг того же URL; кросс-источниковые перепечатки одного события (разные заголовки/тексты) не ловит ничто, кроме ручной пометки «Дубликат». Пример 25.07: один инфоповод «норматив реализации топлива на бирже 2%» пришёл от 3 источников (Газбатюшка/Energy Today/Интерфакс ТЭК) тремя разными текстами. **Дизайн — каскад дёшево→дорого, НЕ LLM-в-лоб (N² пар не масштабируется):** (0) content_hash — есть; (1) сигнатура без AI: SimHash/MinHash **по ТЕЛУ** + числа/сущности; (2) эмбеддинги title+lead + косинус в ОКНЕ (±3–7 дней/по тегу), порог ~0.9 — ловит семантику дёшево (~$0.00002/шт); (3) LLM-арбитр ТОЛЬКО для серой зоны косинуса (единицы вызовов), не основной механизм. ⚠️ **ЗАМЕР 27.07 — две поправки к дизайну, обе load-bearing:** (а) **дедуп по ЗАГОЛОВКУ — ловушка, из уровня 1 убран.** Совпадение нормализованного заголовка даёт 172–286 «дублей», но это почти сплошь СЕРИЙНЫЕ заголовки одного источника, а не перепечатки: Neftegaz — 36 статей «Не было времени смотреть ленту?» = 36 РАЗНЫХ ежедневных топ-10 (36 разных текстов, 36 URL); КазМунайГаз — «Производственные результаты…» ×8, тоже 8 разных отчётов. Схлопывание по заголовку УНИЧТОЖИЛО бы сотни настоящих статей. Сравнивать только тело/семантику. (б) **`pgvector` на проде ОТСУТСТВУЕТ** (`postgres:16`, доступен лишь `pg_trgm`). Варианты: сменить образ на `pgvector/pgvector:pg16` (миграция на 1.9 ГБ RAM — увязать с №10) либо хранить вектор как массив и считать косинус в приложении по окну (сотни кандидатов — приемлемо, инфру не трогает). **✅ ПРОБА ПРОВЕДЕНА 27–28.07** (732 статьи за неделю, окно ±3 дня, 212 тыс. сравнений; скрипты — `scratchpad/dedup/`). Итоги, определяющие механизм: **(1) Лексика (TF-IDF по телу) НЕ ГОДИТСЯ** — на кейсе владельца «норматив топлива 2%» (9 статей, 5 источников) косинус всего **0.11–0.47**: разные редакции пишут об одном факте своими словами. **(2) Эмбеддинги (`text-embedding-3-small`) РЕШАЮТ** — тот же кейс даёт **0.754–0.842**. **(3) Наивная кластеризация ОПАСНА:** одиночная связь склеила 7 сводок «Топливо в Севастополе по QR-кодам» (вт–пн, разные дни) и 11 котировок Brent ($90.34→$100→$98.83) — это разные события. **(4) Две защиты решают:** только КРОСС-ИСТОЧНИКОВЫЕ пары (внутри источника совпадение = серийная рубрика) + guard по числам (разные значения в заголовках = разные события). С ними на пороге **0.85** — 6.6% избыточности, кластеры чистые (проверено глазами: «Роснефть ПНГ 53 млрд» ×4 источника, «Запрет на экспорт бензина» ×4, «ADNOC FID Umm Shaif» ×3); на **0.80** — 12.3% и кейс владельца собирается целиком, макс. кластер 7. **Рекомендация:** авто-схлопывание при ≥0.85, полоса 0.75–0.85 → LLM-арбитр (уровень 3), это ~140 пар/неделю = копейки. **Экономика:** эмбеддинги ~265 статей/день × 400 токенов ≈ **$0.002/день** — фактически бесплатно. Осталось решить: pgvector-образ или косинус в приложении (окно даёт сотни кандидатов — приложения хватает). **Отдельно от обнаружения — ПОЛИТИКА (продуктовое решение):** какой оставить главным (первоисточник/полный текст/высший балл — в примере Интерфакс ТЭК 2496 симв vs Energy Today «неполный текст» 234), схлопывать в duplicate или группировать в ленте «+N перепечаток». ⚠️ авто-схлопывание не должно затирать чужой ручной статус digest/review (пер-юзерность, см. №19/T16). Надстройка над №19. **➡️ НАПРАВЛЕНИЕ ВЛАДЕЛЬЦА 02.08: дедуп вынести В НАЧАЛО конвейера (после определения сути), а не делать отдельным проходом по базе.** Согласен с направлением, три поправки: **(а) сдвинуть ещё на шаг раньше — сразу после гейта релевантности, ДО сути.** Проба мерила косинус на паре «заголовок + начало текста», а не на LLM-сути; на этом входе порог **0.85 применим как есть**. Если считать эмбеддинг по `summary`, вход другой и порог надо калибровать заново — а скрипты пробы лежали в эфемерном scratchpad и **не сохранились**, то есть перезамер = повторный прогон с нуля. Плюс экономия: дедуп до сути срезает 4 платных вызова из 5, после сути — только 2. **(б) «Первым пришёл — тот и главный».** В конвейере статья видит только окно НАЗАД, поэтому если агрегатор опередил первоисточник, главным станет агрегатор. Либо принимаем это правило явно, либо нужен пересмотр главного задним числом — решить до реализации. **(в) Балла ещё нет.** Дедуп до скоринга ⇒ выбирать главного по «высшему баллу» нельзя; критерии — длина/полнота текста и приоритет источника. **⚠️ Порядок: только ПОСЛЕ скрытия 662 из №24** — иначе дедуп схлопнет испорченные статьи как «дубликаты» и законсервирует порчу **Подтверждение от заказчика 22.08:** Виктор прислал скриншот с четырьмя статьями об ОДНОМ событии (повторный ГРП в Югре) из трёх источников с баллами 86, 88, 79 и 71 — «дедупликацию точно надо проверить». Отдельно прислал пару, где один материал заведён дважды и получил РАЗНЫЕ баллы 84 и 88 (заведено отдельно как GitHub #53 — там дело не только в дубле, но и в невоспроизводимости балла). ⚠️ Порядок работ не меняется: сначала #52 (чужая суть), иначе дедуп опознает испорченные статьи как дубликаты и СПРЯЧЕТ порчу вместо починки.| 🔵 | 2026-08-22 |
| 22 | **P2** | **Месячные архивы сигналов.** Сейчас есть только «корзина» (7403 нерелевантных, `pending_deletion`), а сигналы прошлых месяцев остаются в общей ленте вперемешку с текущими. Нужно: сигналы закрытого месяца уезжают в «Архив сигналов — июнь», «…— май» и т.д., лента показывает текущий месяц. **⚠️ Две развилки, вскрытые замером 25.07 — решить ДО реализации:** (1) **статус `archive` УЖЕ ЕСТЬ** и это пер-юзерный ручной статус (3 статьи используют). Месячный архив — другая сущность: автоматическая и глобальная. Переиспользовать тот же статус нельзя — ручной архив пользователя станет неотличим от авто-архивации; нужно отдельное измерение (вычисляемый период по дате + фильтр-переключатель в ленте, а не запись статуса). (2) **По какой дате резать:** в базе 144 месяца, включая 2014–2021 (по 2–20 статей); реальная лента — 2026-06 (2356) и 2026-07 (2417). Если резать по `published_at`, в «архив» мгновенно улетят сотни древних статей, собранных на этой неделе. Вероятно, резать по `collected_at` (когда попало к нам) либо по обеим датам с приоритетом сбора. **Рекомендация:** делать архив ПРЕДСТАВЛЕНИЕМ (фильтр по периоду поверх существующей ленты), а не массовой простановкой статусов — тогда не конфликтует с пер-юзерными статусами, обратимо и не трогает данные. **➡️ ТРЕБОВАНИЕ ВЛАДЕЛЬЦА 02.08:** лента показывает текущий месяц; прошлые месяцы — отдельной кнопкой наверху «Сигналов», там **только статьи + поиск по фильтрам, без статусов и работы**. Это снимает развилку (1): архив = представление, статус `archive` не трогаем. **⚠️ РАЗВИЛКА (3), НЕ НАЗВАННАЯ В ТРЕБОВАНИИ И КРИТИЧЕСКАЯ — когда месяц закрывается.** Если правило «архив = всё кроме текущего месяца», то 1-го числа месячная работа обрывается на полуслове: сегодня 2 августа, и по этому правилу июль уже архив — **а июльский выпуск ещё не собран**. Дайджест за месяц всегда собирается в начале следующего, то есть правило ломает продукт ровно в тот момент, когда он нужнее всего. **Решение:** месяц уезжает в архив не по календарю, а по первому из двух событий — (i) сохранён дайджест за этот период (`monthly_digests` за месяц существует) либо (ii) прошло N дней после конца месяца (предлагаю 10–15). Тогда сейчас в работе август + июль, а июнь и май уже в архиве. **Развилка (2) — по какой дате резать — решается формулой, а не выбором:** период = месяц `published_at`, НО если `collected_at − published_at > ~45 дней`, то месяц `collected_at`. Иначе статья, опубликованная в мае и найденная в августе, молча уедет в майский архив и её никто не увидит. Месяцы с числом статей ниже порога (тот самый хвост 2014–2021 по 2–20 шт.) не создают кнопок, а собираются в бакет «Ранее». **Реализация:** материализованная колонка `articles.period_month DATE` (заполнение при вставке + разовый бэкфилл) — иначе `date_trunc` в WHERE убивает `idx_articles_published_at`; API `GET /api/articles?period=YYYY-MM&mode=archive` — в архивном режиме не джойнит `user_article_states` и игнорирует статус-фильтры; фронт прячет кнопки статусов, оставляет поиск/фильтры. **При закрытии месяца — отчёт «осталось непристроенным N статей со статусом digest»**, иначе отобранное потеряется молча. Порядок: **после №24 (скрыть испорченные) и №21 (дедуп)** — иначе в архиве законсервируется мусор с чужой сутью | 🔵 | 2026-08-02 |
| 23 | **P3** | **Дайджест-посты агрегаторов протекают в ленту как «сигналы»** (вскрыто замером 27.07 при разборе №21). Neftegaz.ru отдаёт ежедневные сводки «Не было времени смотреть ленту? Тогда читай наш краткий топ-10…» — **36 штук, все прошли гейт релевантности** (`relevant=t`), продолжают идти (последняя 24.07). Проблема тройная: (1) в ленте бесполезный заголовок вместо новости; (2) внутри 10 НЕСВЯЗАННЫХ новостей — тег и скоринг считаются по смеси, искажая аналитику; (3) платим ИИ за обработку сводки, которая дублирует уже собранные из первоисточников новости. Варианты: не забирать такие посты (правило по источнику/шаблону заголовка), либо помечать отдельным типом «обзор» и не пускать в скоринг. Пересекается с №20 (обучающий материал гейта) | 🆕 | 2026-07-27 |
| 24 | **P1 🔴** | **ЧУЖОЙ ТЕКСТ У СТАТЬИ — суть не про заголовок.** Инциденты владельца 16.07 и 28.07 подтверждены замером 28.07: **951 статья** (377 кластеров) хранит `raw_text`, побайтово совпадающий с ДРУГОЙ статьёй при разных URL и заголовках. Крупнейший кластер — **63 статьи Neftegaz.ru, собранные 19.06**, все с одним текстом про «World Oil Outlook ОПЕК». **643 такие статьи ВИДНЫ в ленте, 78 из них с баллом ≥60 — то есть годны в дайджест заказчику.** За их обработку заплачено ИИ, суть/теги/скоринг посчитаны по ЧУЖОМУ тексту. Идёт до сих пор (30 статей на неделе 27.07). Топ: Neftegaz.ru 674 (rss), МФТИ 268 (rss), РБК 54 (request), Petroleum Economist 44 (playwright, ОДИН кластер за 06-09…07-24 — типичный пейвол). **КОРЕНЬ (найден в коде):** `article_fetcher._is_better_text` (`article_fetcher.py:294-301`) решает «лучше ли новый текст» **ТОЛЬКО ПО ДЛИНЕ** — никакой проверки, что текст относится к ЭТОЙ статье. Плюс `_request` идёт с `allow_redirects=True`. Значит если сайт на URL статьи отдал листинг/пейвол/заглушку/редирект на главную — любой достаточно длинный текст побеждает и затирает `raw_text`. Два наблюдаемых профиля это подтверждают: 517 статей в кластерах, растянутых НА ДНИ (пейвол — одна и та же страница на каждый запрос) и 244 в кластерах «один проход <5 мин» (сайт отдавал заглушку в момент серии запросов). Слой БД невиновен — `insert_article` только вставляет (проверено). **⚠️ ДЕФЕКТ ДВУХГОЛОВЫЙ** (разделено по `extraction_method` на проде 28.07 — фиксы РАЗНЫЕ): **Ветка Б — дозагрузчик, 703 статьи** (`extraction_method='lxml'`, `full_text_status='ok'`), почти вся Neftegaz.ru (676). Три независимых механизма в `article_fetcher`, все подтверждены разбором кода + воспроизведены экспериментом на реальном коде: (1) нет проверки, что скачали ИМЕННО эту статью — финальный URL после `allow_redirects=True` нигде не сверяется (`grep` по resp.url и canonical пуст), любой редирект/soft-404/WAF-заглушка даёт один HTML на разные URL; (2) `extract_main_text` при отсутствии семантического контейнера падает на ВЕСЬ документ (`if not candidates: candidates = [doc]`, стр. 212–213) и собирает обвязку шаблона — блок «последние новости», подвал, — ОДИНАКОВУЮ на всех страницах источника; (3) `_json_ld_article_text` берёт САМЫЙ ДЛИННЫЙ `articleBody` из ВСЕХ ld+json на странице (`max(..., key=len)`, стр. 242) без сверки headline/url, а структурному тексту дана фора +300 к скору (стр. 216) — если шаблон вшивает JSON-LD «избранного» материала в каждую страницу, все статьи источника получают ЕГО тело. Плюс пропускной клапан: `_is_better_text` при ПУСТОМ `raw_text` (типично для RSS) вообще не проверяет отношение длин — `if current_len and ...` ложно при 0, и любые 801 симв. считаются улучшением; результат помечается `full_text_status='ok'`, то есть порча записывается как успех и больше не перепроверяется. **Ветка А — парсер при вставке, 593 статьи** (`extraction_method IS NULL` — дозагрузчик их не касался, тело пришло из парсинга): МФТИ 258 (rss), РБК 52 (request), Petroleum Economist 44 (playwright), TechnipFMC 27, Сколково/Новатэк/ЕЭК. Здесь причина иная — тело берётся из фида/листинга и оказывается общим; разбирать отдельно по парсерам. **ФИКС (обе ветки):** guard принадлежности — требовать пересечения значимых слов ЗАГОЛОВКА с текстом + отвергать текст, побайтово равный тексту другой свежей статьи того же источника (дешёвый хэш-guard закрывает СРАЗУ ОБЕ ветки на общем пути записи); для ветки Б дополнительно сверять финальный URL/canonical и не давать JSON-LD побеждать без совпадения headline. **⚠️ БЛОКИРУЕТ №21:** эти 951 статья с одинаковым телом будут опознаны дедупом как «дубликаты» — дедуп СПРЯЧЕТ порчу вместо починки. Чинить до включения дедупа. Плюс нужен разовый пере-сбор испорченных · **Где в коде:** `ingestion/article_fetcher.py:294`, `ingestion/http_client.py` · **🔵 ФИКС ЗАДЕПЛОЕН 28.07 (`1c33303`)** — новая порча остановлена: два рубежа в `fetch_article_text` (пересечение слов заголовка с телом + «такое тело уже есть у другой статьи этого источника»), отказ даёт статус `mismatch` и НЕ затирает прежний текст. Сравнение посегментное — иначе хвост издания («— Новости о нефти и газе…») топил долю и резал ВЕРНЫЕ статьи OilCapital (19.6%→3.8%). Порог 0.20 подобран замером; на полных текстах вне Neftegaz отвергается 0.42% (22 из 5283). Схема: `articles.body_hash` + индекс `(source_id, body_hash)`, бэкфилл 15 941 статьи выполнен. +10 тестов, 244 зелёных. **⚠️ ОСТАЁТСЯ РЕШЕНИЕ ВЛАДЕЛЬЦА по УЖЕ испорченным (662):** пилот пере-сбора 40 статей показал, что **починить их пере-сбором НЕЛЬЗЯ** — обновилась 1, «слишком коротких» 30, отклонено стражем 9; из 54 тронутых 40 остались с чужим телом. Причина: страж не даёт записать новый мусор, но старый текст не трогает, а сайты (в основном Neftegaz.ru) не отдают извлекаемого текста. Варианты: (а) очистить чужой текст — статья выпадет из ленты сама; (б) скрыть их (`pending_deletion`); (в) оставить как есть — но тогда 78 статей с баллом ≥60 могут уйти заказчику с чужой сутью. ⚠️ Любой из (а)/(б) — разрушающая операция, нужен явный запрос. **➡️ РЕШЕНИЕ ВЛАДЕЛЬЦА 02.08: вариант (б) — скрываем, как нерелевантные.** ⚠️ **Уточнение к «загвоздке про статусы» (проверено по коду 02.08): загвоздок ДВЕ, и они разного знака.** (1) **Сохранённый выпуск уже защищён** — `mark_article_for_deletion` (`repository.py:1775`) гейтит по `monthly_digest_items` и возвращает `skipped_in_digest`; статья, попавшая в сохранённый дайджест, скрыта НЕ будет. (2) **Черновая пер-юзерная разметка НЕ защищена** — `user_article_states.status IN ('digest','review')` не проверяется вообще (остаток №19). **Но для №24 это НУЖНОЕ поведение, а не дефект:** испорченная статья с пометкой «в дайджест» — ровно тот случай, ради которого всё затевалось (она в шаге от ухода к заказчику с чужой сутью). Guard из №19 её бы «спас» и тем самым СОХРАНИЛ риск. Поэтому: скрывать все 662 **включая помеченные**, но обязательно **отчёт поимённо** — какие отобранные статьи скрыты и у кого, чтобы аналитик не обнаружил молчаливую пропажу. Guard из №19 ставить отдельно и только для АВТОМАТИКИ (дедуп №21), где решение принимает машина по эвристике — там ручная разметка человека должна побеждать. Разводить обязательно, иначе guard заблокирует именно те статьи с баллом ≥60, ради которых задача и заведена. **✅ ЗАМЕР НА ПРОДЕ 02.08 — путь свободен:** испорченных видимых **454** (было 662 на 28.07 — за пять дней часть пере-собралась корректно уже под стражем, то есть страж не только держит оборону, но и медленно лечит), из них с баллом ≥60 — **48**, **размечено `digest`/`review` — 0**. Опасение про статусы на практике пустое: скрытие 454 не заденет ничью работу. **✅ ВЫПОЛНЕНО 03.08 — ЗАДАЧА ЗАКРЫТА.** Скрыто 455 (454 прогнал владелец причиной `wrong_body_guard_2026-08-02`, +1 добрана позже причиной `wrong_body_2026-08-03`). Контроль после операции: **из сохранённых выпусков не скрыто ни одной** — guard по `monthly_digest_items` отработал; 2 скрытые статьи с ручным `review` к операции отношения не имеют, у них причина от гейта релевантности. Видимых сигналов 6889, из них ≥60 баллов — 1178; скрытых всего 7858. Списки id для отката: `exports/wrong-body-hidden-*.json`. ➡️ **Снят блокер с №21 — дедуп можно включать** ⚠️ **ПЕРЕОТКРЫТА 22.08 по наблюдению заказчика (GitHub #52).** Виктор прислал скриншоты: две статьи Neftegaz.ru от 18.08 («Chevron… шельф Анголы» 4375 симв. и «Во Франции… электроэнергия» 4374 симв.) несут суть про керамические микросферы ПНИПУ, оригинал — EnergyLand 17.08. Даты ПОСЛЕ фикса 28.07, баллы 85 и 87 — то есть статьи годны в дайджест с чужой сутью. **Разбор кода показал, что фикс покрывает один путь из шести.** `_ownership_rejection` вызывается только в `article_fetcher.py` (строки 171 и 182) — это дозагрузчик. А статьи пишутся через `repository.insert_article`, который зовут пять модулей приёма: `rss_parser` (по нему работает Neftegaz.ru), `request_parser`, `external_fetch`, `telegram_parser`, `manual_import`. В самом `insert_article` (repository.py:1231) проверок принадлежности НОЛЬ — он только считает и сохраняет `body_hash`. Запись выше о том, что хэш-guard «закрывает СРАЗУ ОБЕ ветки на общем пути записи», описанной реализации не соответствует. **Вторая причина:** `compute_body_hash` это sha256 от текста, разница в ОДИН символ даёт другой хэш — 4375 против 4374 показывают, что рубеж не сработал бы и при вызове. Точное равенство ловит только идеальный случай. ➡️ Чинить переносом проверки на общий путь записи плюс переход с точного хэша на префиксное сравнение. **✅ ПОДТВЕРЖДЕНО ЗАМЕРОМ НА ПРОДЕ 22.08, масштаб больше ожидаемого.** Среди статей, собранных ПОСЛЕ стража и видимых в ленте, общее начало тела (400 символов) с другой статьёй того же источника имеют: РБК Энергетика 109 (29.07–22.08), МФТИ 90, Neftegaz.ru 55 + 42, Новатэк 30 (05.08–22.08), УГНТУ 18, Petroleum Economist 17, TechCrunch 11, Интерфакс ТЭК 10, Upstream Online 6 — **388 статей в топ-10, верхняя граница периода — сегодняшний день.** Порча НЕ остановлена, идёт прямо сейчас. ⚠️ Оговорка до массовых операций: для Neftegaz и Petroleum Economist профиль совпадает с замером 28.07 и это почти наверняка чужое тело, а для РБК, МФТИ и Новатэка возможна безобидная шаблонная преамбула — смотреть глазами до того, как что-то скрывать. **Побочно найдено: Neftegaz.ru заведён ДВУМЯ источниками (id 36 и 53) — GitHub #59; это ломает кросс-источниковую защиту дедупа №21 и занижает масштаб самого замера.**| 🔵 | 2026-08-22 |
| 26 | **P1 🔴** | **Удаление соцсетей в сборщике не сохранялось — ДВЕ независимые причины** (жалоба заказчика 03.08, при подготовке июльского выпуска к публикации). **(1) Код ✅ ИСПРАВЛЕНО:** `save_digest_branding` (`digest.py:198`) сливал соцсети через `payload_socials or current_socials` — пустой список в Python ложен, поэтому осознанное «удалить все» проваливалось на прежнее значение и ссылки возвращались. Теперь различается «ключа нет» и «ключ пустой»; +2 регресс-теста. **(2) Инфраструктура — ОТКРЫТО:** `digest_branding.json` лежит в `oiltech_digest/processing/`, **под git и внутри образа**, тома на него нет (`docker-compose.yml` монтирует только `BACKLOG.md` и `exports`). Любой деплой `git reset --hard` + `build` возвращает git-версию файла — а в ней записаны ровно те 6 ссылок (VK, Telegram, YouTube, RT, Дзен, Энергия+) — и заодно затирает ВСЕ прочие правки брендинга заказчика (шапка, hero, футер, контакты). Тот же класс, что T11. ⚠️ Перед ближайшим деплоем обязательно снять копию прод-файла и вернуть после. Решение: вынести брендинг из образа — путь через env-переменную + именованный том (в репо-каталог класть нельзя, `reset --hard` затрёт так же). **✅ Код задеплоен 03.08 (`4521d73`).** Сверка перед деплоем показала, что прод-файл был побайтово равен git-версии — удаление не сохранялось НИ РАЗУ. **(3) ТРЕТЬЯ ПРИЧИНА, вскрыта после первого фикса:** заказчик сообщил, что ссылки пропадают в превью, но остаются в выгрузке — и в HTML, и в PDF. Замер по контейнерам: `app` → `socials []`, а `worker` / `playwright-worker` / `scheduler` → прежние шесть. Файл лежал в файловой системе КАЖДОГО контейнера (`COPY oiltech_digest` в образ), общего тома не было: админка писала в свою копию, а **выгрузку делают воркеры** и читали свою, нетронутую. Превью отдаёт `app` — поэтому и расходилось. **✅ ЗАКРЫТО ПОЛНОСТЬЮ (`bf01779`):** `config.DIGEST_BRANDING_PATH` + именованный том `branding`, смонтированный в app / tasks / worker / playwright-worker / scheduler; чтение с отпаданием на упакованный дефолт при пустом томе. Это же закрывает и пункт (2) — том переживает `reset --hard` и `build`. Проверено: все 5 контейнеров читают один файл, в выгрузке блока `footer-social` нет. +2 теста на совпадение путей записи и чтения · **Где в коде:** `processing/digest.py`, `config.py`, `docker-compose.yml` | ✅ | 2026-08-03 |
| 27 | **P1 🔴** | **Картинки новостей в дайджесте: чужие/битые изображения уходят в выпуск** (жалоба заказчика 03.08 — в июльском выпуске негодны 5 из 6 новостей: №1, 3, 4, 5, 6). **Корень в коде:** `_is_unusable_digest_image_url` (`digest.py:688`) отбраковывает только пустое, нестандартную схему и `example.com`/`localhost`. НЕ проверяется ничего содержательного: жив ли адрес, картинка ли это вообще, и не является ли она общим баннером/логотипом источника. `image_url` берётся из og:image страницы либо из RSS-энкложура — у многих изданий там один и тот же шаблонный баннер на все статьи. Заглушка по категории (`_news_placeholder_data_uri`) уже есть и работает, но включается ТОЛЬКО при пустом `image_url`. **Фикс — по образцу стража №24:** (а) guard «одна и та же картинка у N статей источника = это шаблон издания, а не иллюстрация статьи» (дешёвый счётчик по `(source_id, image_url)`, ровно как `body_hash`); (б) проверка, что адрес реально отдаёт `image/*`; (в) при отказе — штатная заглушка. **✅ ЧАСТЬ (в) СДЕЛАНА И ЗАДЕПЛОЕНА 03.08 (`4521d73`)** — разбор показал, что заглушка была виновата не меньше картинок: словарь палитры имел ключи «технологии»/«рынок», а реальные теги нормализуются в «роботизацияиавтономныесистемы» — **не совпадал ни один ключ**, поэтому ВСЕ заглушки выпуска брали цвет по умолчанию и выглядели как одинаковые тёмно-синие прямоугольники. Теперь цвет детерминирован по хэшу рубрики, подпись прижата к низу под затемняющей подложкой, кегль 7.5→9.5, две строки вместо трёх, обрыв помечается многоточием (прежняя выдавала «КОНТРАКТЫ M A»). Разбор конкретного выпуска (id=4, Родионов, 7 новостей): жалобы {1,3,4,5,6} совпали с множеством {заглушка} ∪ {картинка не с spe.org} день в день — №3 фото буровой с разъехавшимися цветами, №4 вообще не иллюстрация, а репост-карточка Интерфакса с логотипом и тем же заголовком текстом. У обеих `image_url` очищен вручную (откат записан в переписке), выпуск пересобран. **ОСТАЁТСЯ (а) и (б)** — guard «одна картинка у N статей источника» и проверка `image/*`; отдельно: og:image Интерфакса (`interfax.ru/aspimg/*`) системно бесполезен, это шаблонная карточка-репост. ⚠️ `backfill_images` берёт статьи с ПУСТЫМ `image_url` — ручной прогон вернёт очищенные картинки обратно; в цикле scheduler его нет, но помнить · **Где в коде:** `processing/digest.py:688`, `ingestion/article_fetcher.py` | 🔵 | 2026-08-03 |
| 25 | **P2** | **Автообучение гейта на пометках «Шум»/«Дубликат»** (требование владельца 28.07). Пометки людей должны становиться обучающим материалом **автоматически**, система сама делает выводы и применяет к отбору. Отличие от №20: тот — разовый ручной разбор, здесь — постоянный контур обратной связи. Объём: 112 «шум» + 44 «дубликат», растёт. **Открытые вопросы:** (1) механизм — правила в промпт, few-shot ближайшими примерами (ложится на эмбеддинги №21) или классификатор поверх эмбеддингов; (2) защита от порчи гейта — замер до/после на фиксированной выборке + откат; (3) чья разметка авторитетна (заказчик Родионов id4 > админы, админскую 24.07 сняли как шумную); (4) «Дубликат» — сигнал НЕ для гейта релевантности, а для порога №21 · **Где в коде:** `processing/pipeline.py`, `user_article_states` | 🆕 | 2026-07-28 |
| 28 | **P1** | **Приём файлов: загрузка PDF/PPTX/DOCX → карточка документа → корпус материалов.** Спека — GitHub #31, нарезка — #32–#41. **✅ НА ПРОДЕ с 21.08.** Работает: загрузка одного файла и пачки, личный список, скачивание оригинала; извлечение текста с якорями на страницу/слайд (pypdf + python-pptx + python-docx, сканы отклоняются с предупреждением); ИИ-разбор — паспорт документа, суть, сводка, факты с номером страницы, заявления отделены от фактов; **сверка каждого числа с текстом страницы прямо в конвейере, а не тестом**. Первый реальный документ (обзор «АТОН», 16 страниц): 42 факта, 38 подтверждено — 90%. Замеренная стоимость: 13-слайдовая презентация $0,0195; 60-страничный PDF ~$0,17. Парсер выбран замером трёх библиотек на 299-страничном PDF: pypdf 35 МБ пик против 511 МБ у pdfplumber (на 1,9 ГБ сервере — готовый OOM), pymupdf быстрее всех, но AGPL — лицензионный риск в поставке ГПН. Загрузка **только администратору**: так открытая регистрация (#33) не даёт постороннему жечь бюджет OpenAI. Документы личные, в дайджест НЕ идут, балл по своей шкале — новостной скоринг надувал бы балл длиной документа. ⚠️ **Осознанное отступление от спеки:** разбор идёт инлайном в веб-процессе, а не фоновой задачей (#44) — тип задачи без локального обработчика не откладывается, а гибнет. **Открыто 13 тикетов:** #38 балл · #39 направления и компании · #40 корпус · #42 частичные результаты · #43 пакетная загрузка и квоты · #44 разбор в фоне · #45 OCR · #46 приём почтой и телеграмом · #47 прогресс разбора · #48 выгрузка CSV · #49 поиск и сортировка · #50 дата из имени файла · #51 таблицы документов в проверку готовности | ✅ | 2026-08-22 |
| 29 | **P1 🔴** | **Дайджест для ДСА (заместитель Дюкова) — показ примерно 03.09.** Задача от Виктора 20.08: «через две недели ОС у ДСА, надо собрать первую версию дайджеста показать, задача очень серьёзная». Требования дословно: «он должен быть вылизан», перевод, дизайн, ссылки, графика, изображения, и вытащить HTML, чтобы можно было перейти и прочитать новости. Выборку новостей Виктор берёт на себя. **Упирается в качество ленты:** GitHub #52 чужая суть · #53 дубли и невоспроизводимый балл · #54 инциденты у порога 60 · #55 склеенные заголовки · №21 дедуп. Это одна работа, а не две. ⚠️ Конфликт с планом: `docs/SPRINT_PLAN.md` на 25.08–05.09 ставит блок «Память рынка: архив», показа ДСА там нет, а срок ближе вехи 31.08. Развилка «какой спринт двигаем» открыта с 20.08. Тикет — GitHub #58 | 🆕 | 2026-08-22 |

---

## 🛠 Технический долг и баги (аудит 2026-06-29)

> Найдено при полном аудите кода + сверке реального счёта OpenAI с токенами из `ai_processing_runs`. Это **открытые** проблемы (на момент фиксации не исправлены).

| # | Приоритет | Проблема | Где | Что сделать | Статус |
|---|:---:|---|---|---|:---:|
| T1 | **P1 🔴** | **IDOR / приватность:** любой залогиненный пользователь по номеру задачи скачивает чужой персональный дайджест-PDF и читает чужой payload (выбор статей, user_id) | `api.py` `/api/jobs`, `/api/jobs/{id}`, `/api/jobs/{id}/download`; `background_jobs.user_id` | ✅ Закрыто: у задач есть владелец, user endpoints фильтруют `list/get/download`, admin видит системные задачи | ✅ |
| T2 | **P1 🔴** | **Двойной AI-расход (TOCTOU):** при истечении lease внешнего воркера gpt-5.5-задача переотдаётся и применяется повторно → OpenAI биллит дважды. Сверка подтверждает: реальный счёт $193.6 против записанных $95.3 (**×2**) | `api.py` `external_worker_complete`; `repository` `requeue_expired_external_leases`/`finish_external_background_job` | ✅ Закрыто 01.07 (`ccbcf69`, PR #20): finalize атомарен — задача уходит в `finalizing` до apply, requeue её не переотдаёт | ✅ |
| T3 | **P1 🟠** | **Мягкое удаление recheck не задеплоено на прод:** до деплоя повторный `recheck` снова удаляет статьи безвозвратно (первый прогон уже срезал ~2000) | прод; код в `main` (`recheck --mark`/`recheck-marked`/`purge`/`unmark`) | ✅ Закрыто 17.07 (PR #25): читали `payload` вместо `payload_json` → `--mark`/`--dry-run` игнорировались и статьи удалялись ФИЗИЧЕСКИ. Мягкий режим работает после деплоя | ✅ |
| T4 | **P2 🟡** | **Экран «AI-затраты» врёт:** `cost_usd` считается одним прайсом `0.05/0.40` (это прайс gpt-5-nano) для всех моделей → занижает gpt-5-mini ~15×, gpt-5.5 ~100× | `processing/openai_client.py` `cost_usd`; `config.py` `OPENAI_*_USD_PER_MTOK` | ✅ `config.price_for_model()` — пер-модельная таблица; `cost_usd` берёт ставку по модели. Пересчёт истории на проде ($5→$245 реальных). Forward на NL-воркере — после его редеплоя (SSH трансгранично закрыт) | ✅ |
| T5 | **P3 🟡** | **Регрессия модели:** статьи обработаны слабой `gpt-5-nano` (забыт `OPENAI_MODEL` на внешнем воркере) — бьёт по качеству выборки. **Замер на проде 25.07:** ровно **1012 статей** (4038 прогонов, `gpt-5-nano-2025-08-07`); утечка ПРЕКРАЩЕНА — последний nano-прогон 25.06, новых нет. Осталась только историческая порча | внешний воркер; `ai_processing_runs` model=`gpt-5-nano-*` | **✅ ВЫПОЛНЕНО 27.07** (задачи 1311–1320, 14:23–20:56): перепрогнано **942 статьи** из 992 живых (15 уже вычищены + 5 отклонены — их не гоняли, деньги на ветер), **$14.85** при оценке $17.86. Модели корректные: `gpt-5.4` гейт + `gpt-5-mini` суть/теги/скоринг/перевод. Гейт переклассифицировал 132 статьи в нерелевантные — лента почистилась. **Остаток 48** упал на преходящих ошибках (7× read timeout к OpenAI, остальное `max_output_tokens` — модель упёрлась в потолок вывода): догон задачей **1348**. ⚠️ Урок: `attempts=ok` у задачи ≠ содержимое обновилось — сверять по `ai_processing_runs.job_id`, а не по окну времени | ✅ |
| T6 | **P3 🟡** | `monthly_digests` ключуется по месяцу без `user_id` — драфты разных пользователей затирают друг друга (рассинхрон с пер-юзерной моделью) | `processing/digest.py` `save_digest_draft`; `repository` `save_monthly_digest` | ✅ Закрыто: `monthly_digests` получил `user_id`, уникальность по `(user_id, month)`, API читает/сохраняет драфты в срезе текущего пользователя | ✅ |
| T7 | **P3 🟡** | `/api/process` и `/api/jobs/process` доступны любому пользователю → любой тратит платный OpenAI | `api.py` | ✅ Оба эндпоинта → `require_admin` (задеплоено на РФ-core) | ✅ |
| T8 | **P3 🟡** | Сессионная cookie без флага `Secure` (прод по HTTPS) | `auth.py` / выставление cookie | ✅ `secure=config.AUTH_COOKIE_SECURE` (дефолт True; локалка http → `AUTH_COOKIE_SECURE=0`). Задеплоено | ✅ |
| T9 | **P4 ⚪** | **Сверка 25.07 сняла 2 пункта из 3 как несуществующие:** (1) «двойной `_register_block` удлиняет cooldown» — премисса НЕВЕРНА: cooldown абсолютный (`monotonic()+cooldown`, `http_client.py:373`), не аддитивный, и в SSL-цепочке блок регистрируется максимум раз; (2) «`insert_candidates` без границы» — граница фактически есть: `REQUEST_ARTICLE_LIMIT=6` + усечение во всех трёх вызовах до передачи. **ОСТАЁТСЯ только (3):** `source-health stale_days=3` шумит на редко-публикующих — плоский порог для всех источников (`repository.py:1618`). Поднять дефолт до 7–14 либо сделать cadence-aware (сравнивать с медианным интервалом самого источника) | `repository` `source_health_report` | Одна правка вместо трёх | 🆕 |
| T10 | **P2 🟡** | **Слепая зона учёта AI:** `apply_recheck_result` писал `_insert_run` только в ветке `relevant=true` → гейт-вызовы по ОТКЛОНЁННЫМ статьям (~40% базы) и весь `dry_run` не попадали в `ai_processing_runs`. Сверка с дашбордом OpenAI 23.07: разрыв до **4.5×** (записано 625 вызовов вместо ~2500) | `processing/external_ai.py` | ✅ Закрыто 23.07 (`a0cac1d`): прогон пишется ДО ветвления по вердикту; +3 регресс-теста (их не было вовсе — так баг и выжил) | ✅ |
| T11 | **P2 🟠** | **Трекер задач хранит данные в `BACKLOG.md` — git-файле.** Любой деплой (`git reset --hard origin/main`) молча сотрёт задачи, созданные через прод-UI. **Подтверждено 25.07:** `backlog.py:12` `BACKLOG_PATH=<repo>/BACKLOG.md`, весь CRUD через `read_text`/`write_text`; БД-хранилища нет (grep по `db/` пуст). ⚠️ Риск реализовался бы вчера: деплой 24.07 делал `reset --hard` | `oiltech_digest/backlog.py`; `docker-compose.yml` | Развилка для владельца/автора (electromop): (а) перенести в таблицу БД, либо (б) вывести файл из-под git (`git rm --cached` + `.gitignore`) и сменить деплой-механику | 🆕 |
| T12 | **P3 🟡** | Из docker-сборки убран `tsc -b` → ошибки типов фронта пройдут в прод молча. **Подтверждено 25.07:** `Dockerfile:13` = `RUN npm exec vite build` (без тип-чека), при этом `package.json:8` build = `tsc -b && vite build` — то есть гейт есть в скрипте, но Dockerfile его намеренно обходит. Типы сейчас чисты (проверено локально 25.07) | `Dockerfile` | Вернуть тип-чек ВНЕ сборки образа (пред-коммит или лёгкий CI-шаг), чтобы не воспроизвести OOM на 1.9 ГБ. ⚠️ также `tsconfig.json` даёт TS6310 на `tsc -b` — чинить вместе | 🆕 |
| T14 | **P1 🔴** | **Приватность: `PUT /api/digest-branding` был на `require_user`**, а пишет ОДИН общий файл `digest_branding.json` → любой пользователь менял шапку/hero/футер во ВСЕХ выгрузках у всех, включая уходящую заказчику. Гейт был только во фронте (`isAdmin`) | `api.py:826` | ✅ Закрыто 24.07 (`cedbb72`): `require_admin` + регресс-тест | ✅ |
| T15 | **P1 🔴** | **`/api/maintenance/{cleanup,status,benchmark}` были на `require_user`**, при этом `cleanup_finished_background_jobs`/`cleanup_finished_export_jobs` удаляют строки БЕЗ фильтра по `user_id` → любой пользователь стирал историю задач и выгрузок ОСТАЛЬНЫМ | `api.py:1105,1112,1129`; `repository.py:1077,1108` | ✅ Закрыто 24.07: `require_admin` на все три + регресс-тесты | ✅ |
| T16 | **P1 🔴** | **Чужой дайджест побеждал личный.** `get_monthly_digest`: `ORDER BY (user_id = %s) DESC` — для общей строки `NULL = <id>` даёт NULL, а Postgres в `DESC` по умолчанию **NULLS FIRST** → легаси-строка (`user_id IS NULL`) вставала ПЕРЕД личной и `LIMIT 1` отдавал ЕЁ. На проде такая строка есть (2026-06). Пользователь получал чужую подборку в превью и в PDF/DOCX, а сохранение затирало его личную | `repository.py:2437` | ✅ Закрыто 24.07: `ORDER BY (user_id IS NOT NULL) DESC`. ⚠️ ОСТАЁТСЯ вопрос: нужен ли вообще fallback `OR user_id IS NULL` — сейчас юзер без своего дайджеста видит легаси-общий | ✅ |
| T17 | **P2 🟡** | `digest_items_by_article_ids` принимал `user_id` и НИГДЕ не использовал — ложное ощущение скоупа в цепочке экспорта дайджеста | `repository.py:2457` | ✅ Закрыто 24.07: параметр убран, ответственность за скоуп задокументирована | ✅ |
| T18 | **P2 🟡** | Фронт: `onUnauthorized` чистил только `user`, но не `articles`/`stats` (в `handleLogout` чистка была) → после протухания куки в открытой вкладке следующий вошедший видел кадр с данными прежнего пользователя (его статусы и плитки Шум/Дубликат) | `frontend/src/app/App.tsx` (9 обработчиков) | ✅ Закрыто 24.07: общий `resetSession()` | ✅ |
| T19 | **P3 ⚪** | `cli.py digest-save` вызывает `save_digest_draft` БЕЗ `user_id` → создаёт «общую» строку `monthly_digests(user_id IS NULL)`, питая T16. **Проверено вручную 25.07** (агент сверки упал, перепроверил сам): `cli.py:918` зовёт без `user_id`, у команды нет флага `--user`, в сигнатуре `save_digest_draft` параметр есть и по умолчанию `None` | `cli.py:918` | Добавить `--user` (обязательным) либо запретить сохранение без владельца | 🆕 |
| T20 | **P1 🔴** | **Воркер жёг деньги после потери lease.** `_safe_heartbeat` глушил ЛЮБУЮ ошибку heartbeat, включая 409 (= core отозвал право на работу). Воркер продолжал платные вызовы OpenAI, результат которых гарантированно не примут: ~80 мин × ~1 вызов/2 с ≈ **$11/час в мусор**. Заглушек было ДВЕ (в воркере и в циклах `external_ai`), третий цикл `process_translate_payload` нашёлся только по несошедшемуся счётчику замен | `external_worker.py`, `processing/external_ai.py` | ✅ Закрыто 24.07 (`b71787c`): тип `LeaseLost`, 409 прерывает батч, прочие сбои терпим; при `LeaseLost` не шлём ни `complete`, ни `fail`. +2 теста. NL-воркер пересобран владельцем, петля исчезла. ⚠️ core ещё не задеплоен | ✅ |
| T21 | **P3 🟡** | **Зомби-lease:** `requeue_expired_external_leases()` вызывается ТОЛЬКО из `/api/external-worker/claim`. Пока воркер занят длинной задачей, задачи с протухшим lease висят в `running` и никем не разбираются (24.07 так висели 1182 и 1184). Самолечится при следующем `claim`, но если воркер умрёт насовсем — останутся `running` навсегда, интерфейс будет врать | `api.py:997` | ✅ Закрыто 24.07 (`d3bb6e2`) вместе с T22: реапер вызывается из sweep воркера по расписанию | ✅ |
| T22 | **P1 🔴** | **Вечная петля внешней задачи.** `requeue_stale_background_jobs` возвращал в очередь ЛЮБУЮ `running` старше 60 мин по `started_at`, включая внешние — вопреки живому, только что продлённому lease. Батч 800 статей идёт ~93 мин: на 60-й минуте задача переочередивалась, воркер получал 409, бросал батч и тут же забирал её снова, начиная с первой статьи. Круг ровно в час, вечно, каждый раз заново за деньги. `attempts=6` при `max_attempts=3` — потолок не срабатывал, потому что переочередь его не проверяла. Фильтр «внешний контур не трогаем» стоял только на ветке «пометить failed»; на ветке переочереди был лишь комментарий о намерении | `db/repository.py`, `background_jobs.py` | ✅ Закрыто 24.07 (`d3bb6e2`): часы — запасная эвристика только для контура без протокола живости; у внешнего признак жизни — lease. Реапер по расписанию, `lease IS NULL` = потеря, переочередь сверяется с `max_attempts`, `RequeueOutcome(requeued, exhausted)`. +5 тестов. ⚠️ core не задеплоен | ✅ |
| T23 | **P2 🟠** | **Батч всё-или-ничего.** Результат внешней задачи сохраняется только в `complete`. Теперь, когда lease позволяет батчу идти сколько угодно, радиус поражения поздней ошибки ВЫРОС: батч, умерший на 90-й минуте из 93, теряет всю оплаченную работу и повторяет её целиком | `processing/external_ai.py`, `api.py` | Промежуточная фиксация результата (частичный `complete` либо чекпойнт по N статей) | 🆕 |
| T24 | **P2 🟠** | **`progress` внешней задачи — статичные 20%:** `external_worker.py:139` ставит их при старте и больше не трогает. Система не показывает, что делает: петлю 1181 искали 4 часа вместо 10 минут, потому что «идёт» и «зациклилась» выглядят одинаково. Настоящий счётчик — heartbeat в логах, но это не интерфейс | `external_worker.py` | Слать `progress` раз в N статей из того же колбэка, что и heartbeat. ⚠️ требует пересборки NL-воркера | 🆕 |
| T25 | **P3 ⚪** | `claim_next_background_job` не фильтрует `execution_region` — локальный воркер забрал бы внешнюю задачу, совпади имя очереди. Сейчас безопасно лишь потому, что `BACKGROUND_JOB_QUEUES` = default/playwright | `db/repository.py` | Добавить фильтр по региону, не полагаться на конфиг очередей | 🆕 |
| T13 | **P2 🟠** | **RAM РФ-core на пределе:** свободно ~116 МБ, добавился контейнер `oiltech_tasks`. Сборка при живом стеке рискует OOM (инцидент 03.06) | прод | До переезда (задача 10) — обязательно `docker compose down` перед `build` | 🆕 |

**Оптимизация стоимости** (не баг, но рычаги; полная обработка статьи на mini ≈ $0.007, гейт на gpt-5.5 ≈ $0.011, итого ≈ $0.018/статью): (1) починить T2 = −~50% счёта; (2) гейт на `gpt-5.4` вместо `gpt-5.5` = −50% по гейту; (3) не гонять флагман-гейт по всей базе — инкрементальный гейт по новым статьям почти бесплатен.

---

## ✅ Сделано

| Задача | Когда |
|---|---|
| Приём файлов PDF/PPTX/DOCX: карточка документа с фактами, привязанными к странице, и сверкой каждого числа (№28) | 2026-08-21 |
| Ветка приёма файлов влита в `main` — деплой по инструкции больше не может откатить фичу с прода | 2026-08-22 |
| Чистка репозитория: удалено 11 файлов без единой ссылки, 13 старых хендофов и планов убраны в `docs/archive/` | 2026-08-22 |
| Ревизия источников: 19 правок URL — «замолчавшие» смотрели на карту сайта/главную вместо ленты (PR #29) | 2026-07-20 |
| Парсер срезал финальный слэш → сайты на Bitrix отдавали 404 на каждую статью (PR #30) | 2026-07-20 |
| ИИ снова обрабатывает новые статьи: отклонённые гейтом больше не занимали очередь вечно (PR #26) | 2026-07-17 |
| Мягкое удаление recheck (`--mark`/`--dry-run`) реально работает — T3 (PR #25) | 2026-07-17 |
| Инцидент 10.07: починен Word-экспорт, убрана бесконечная переподгрузка ленты, необработанные статьи снова видны | 2026-07-15 |
| Переезд core в РФ — вход без VPN; гео-архитектура (РФ-core + зарубежный AI-воркер) | 2026-06-19 |
| Мультиюзер + роли admin/user (своё состояние и дайджест) | 2026-06-25 |
| Переводчик заголовков вынесен в отдельную стадию (дайджест всегда на русском) | 2026-06-25 |
| Гейт релевантности ужесточён (отсев СВО/криминал/ДТП/общий шум) + перепрогон базы | 2026-06-26 |
| Мягкое удаление нерелевантных (`--mark`/`recheck-marked`/`purge`/`unmark`) | 2026-06-26 |
| Источники: аудит всех 120; слой фетча вылечен (57→71); 3 SPA→playwright; 2 мёртвых off | 2026-06-26 |
| Стоп-слова (negative keywords) у родительских тегов | 2026-06-25 |
| Word-выгрузка дайджеста на python-docx (бренд, фото, ссылки) | 2026-06-25 |
| Корпоративные шрифты GPN_Din в UI и PDF | 2026-06-25 |
| Сигналы: теги-аккордеон, сортировка по дате, деанглицизация интерфейса | 2026-06-22 |
| SSL + домен `oiltech-digest.ru` (Caddy + авто-HTTPS Let's Encrypt) | 2026-06-05 |
| Бэклог в репозитории (этот файл) | 2026-06-05 |
| Дашборд: честная статистика `/api/stats` (раньше упиралось в «1000») | 2026-06-05 |
| «Все статьи»: показ всех записей + кнопка «Показать ещё» | 2026-06-05 |
| Источники: полный список в фильтре (раньше показывалось 10) | 2026-06-05 |
| Дайджест: фикс «лесенки» текста в превью | 2026-06-05 |
| Будущие даты («инциденты»): гард + флаг + SQL-вычистка на проде | 2026-06-05 |
| Дайджест: фирменный шаблон + экспорт PDF / Word | 2026-06-05 |
| Картинки в дайджесте: og:image из статьи + фирменная заглушка | 2026-06-05 |
| Чистка репозитория + README для пользователей | 2026-06-05 |
| Источники: упрощённые карточки и React-экран управления | 2026-06-06 |
| Дайджест: отдельные кнопки PDF / DOCX / HTML и скачивание без пустой вкладки | 2026-06-06 |
| Новая React-админка: сигналы, дайджест, источники, скоринг, теги | 2026-06-06 |
| Сигналы: честная метрика «Обработано» в `/api/stats` | 2026-06-06 |
| Навигация: переработанный сайдбар с группами «Работа / Настройки» | 2026-06-06 |

---

## 📥 Входящие — пишите сюда

Добавьте строку: дата · автор · что хотите изменить/добавить. Команда перенесёт в план выше и проставит статус.

| Дата | Автор | Комментарий / пожелание | Статус |
|---|---|---|:---:|
| 2026-06-05 | _пример_ | _«Хочу фильтр по дате в выгрузке дайджеста»_ | 🆕 |
|  |  |  |  |
|  |  |  |  |
